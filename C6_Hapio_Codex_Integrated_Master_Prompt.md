> **공개 이관본 안내:** 이 문서의 개인 경로·서명 식별자는 예시로 일반화했습니다. `/path/to/...`, `YOUR_TEAM_ID`, `HOST_IPV4`는 자신의 환경 값으로 바꾸세요. 원시 증거와 과거 문서 묶음은 공개하지 않으며 [검증 요약](docs/VALIDATION_SUMMARY.md)·[이관 범위](docs/MIGRATION.md)에서 구분합니다. 과거 기록의 완료·미완료 상태는 새 실행 결과가 아닙니다.

# C6 · 합이오 — Unity → iOS MVP 통합 Codex 마스터 프롬프트

> **Requirement Intake → Validation → Task Implementation → iPhone Verification**  
> 통합 작성일: 2026-09-11 · 통합본 v1.0  
> 기준 골격: `C6_Codex_Prototype_Prompts.md`의 G0~G6 / T00~T16  
> 상세 요구사항: `hapio_ios_mvp_master_prompt.md`의 합이오 1차 MVP PROJECT INPUT  
> 이 파일은 개발 지시서다. Unity 프로젝트, 실행된 테스트 결과, 실기기 검증 완료 보고서가 아니다.

## 문서 검토 현황

> 검토 반영일: 2026-09-12 · 검토본 r03  
> **검토 01~02는 문단별로 확인했고, 검토 03과 나머지 본문은 사용자의 “나머지도 다 괜찮은듯” 의견에 따라 현행 내용 유지로 일괄 반영했다.**  
> 문단별 확인은 여기서 종료한다. 나머지 문단을 각각 검토하거나 기술적으로 검증했다는 뜻은 아니다.  
> 이번 반영에서는 게임 사양·수치·Task·Acceptance Test를 변경하지 않았다. 문서 검토 현황과 통합 결정의 승인 상태 안내만 갱신했다.  
> 문서 내용 유지 동의와 구현·컴파일·빌드·실기기 검증 완료는 별개다. 실제 실행 증거 없이 검증 상태를 PASS로 바꾸지 않는다. 기존 `DEMO_TUNING_VALUE`와 `DEMO_ASSUMPTION`의 구분도 유지한다.

| 검토 번호 | 위치 | 상태 | 반영 내용 |
|---|---|---|---|
| 01 | 1.1 「이 파일 하나로 사용한다」 첫 문단 | 문단별 사용자 확인 완료 | 통합 Markdown 파일 하나를 Unity 프로젝트 루트에 둔다. 기존 문구 유지. |
| 02 | 1.1 「처음 한 번 Codex에 보낼 요청」 코드 블록의 첫 문단 | 문단별 사용자 확인 완료 | 문서 읽기와 현재 저장소·문서 확인 지시. 기존 문구 유지. |
| 03 | 1.1 「처음 한 번 Codex에 보낼 요청」 코드 블록의 둘째 문단 | 현행 내용 유지에 일괄 동의 | 기존 작업 보존·규칙 병합·검증 증거 확인·첫 요청의 구현 제한. 기존 문구 유지. |
| 이후 | 나머지 본문 | 현행 내용 유지에 일괄 동의 | 게임 사양·Task·테스트 기준 유지. 개별 문단 확인이나 실제 기술 검증으로 기록하지 않음. |

## 문서 구성

1. 사용 방법과 통합 결정표
2. 통합 PROJECT INPUT: 목표·규칙·입력·상태·설정·화면·네트워크
3. Requirement Gate와 작업 역할
4. Codex 공통 작업 지시
5. G0~G6 / T00~T16 실행 순서와 완료 조건
6. Task별 실행 프롬프트
7. 실기기 테스트 시나리오 A~D
8. AT-01~AT-18 및 보강 검증
9. 오류 수정·실기기 결과·리뷰·기록 템플릿
10. 완료 정의·최종 보고·원문 대응표

---

# 1. 사용 방법과 통합 결정

## 1.1 이 파일 하나로 사용한다

이 파일을 Unity 프로젝트 루트, 즉 `Assets`, `Packages`, `ProjectSettings`와 나란히 둔다. 기존 ZIP이나 개별 프롬프트 파일은 필수가 아니다. 실제 저장 파일명을 바꿨다면 아래 요청의 파일명도 맞춘다.

처음 한 번 Codex에 보낼 요청:

```text
C6_Hapio_Codex_Integrated_Master_Prompt.md를 읽어.
특히 1장의 통합 결정표, 2장의 PROJECT INPUT, 4장의 공통 지시,
5장의 Task·실기기 검증 순서를 기준으로 현재 저장소와 문서를 확인해.

기존 AGENTS.md와 사용자 변경은 덮어쓰지 마.
충돌 없는 공통 규칙을 병합하고, 이번 통합본의 적용 차이를 기록해.
기존 G0/G1 또는 다른 Task를 이미 수행했다면 파일과 검증 증거부터 확인해.
이번 요청에서는 지시 적용과 진행 상태 확인만 하고 게임 구현은 시작하지 마.
```

개별 Task 요청:

```text
C6_Hapio_Codex_Integrated_Master_Prompt.md의 공통 규칙과
T02 프롬프트를 읽고 T02만 수행해.
선행 조건은 docs/VALIDATION.md의 실제 증거로 확인해.
코드 변경·테스트·Xcode 프로젝트 생성·iPhone 실행 결과를 구분해 보고해.
다음 Task는 자동으로 시작하지 마.
```

T10은 범위가 커지는 것을 막기 위해 `T10-A`와 `T10-B`를 별도로 요청한다. 상위 Task 번호는 기존 T00~T16의 17개를 유지하지만, T10은 두 번에 나눠 실행한다.

파일 전체를 붙여넣고 “전부 구현”하도록 요청하지 않는다. 하나의 Task를 마친 뒤 변경·검증 결과를 확인하고 다음 작업을 요청한다. 필수 iPhone 검증 지점에서는 실제 결과가 확보될 때까지 다음 G단계로 넘어가지 않는다.

## 1.2 통합 기준

두 원문은 같은 사양이 아니다. **작업 순서와 안전장치는 기존 C6 문서를 유지하고, 이번 게임의 상세 요구사항은 합이오 문서의 명시적인 MVP 결정을 우선 적용했다.** 기존 C6의 3인·자동 회복·수치 등은 검증용 제안이었으므로 합이오의 명시 사양으로 교체한다.

단, 기존 사용자가 정한 “방어는 변수만”과 “공격 영역으로 드래그하면 3D로 전환”은 축소 범위를 유지한다. 이 두 항목과 생명주기 표현 등의 차이는 아래에 따로 표시했다. 아래 채택안은 통합 당시의 편집 결정으로 작성됐으며, 2026-09-12 사용자가 나머지 문서의 현행 내용 유지에 일괄 동의했다. 이는 구현이나 실기기 검증이 완료됐다는 뜻은 아니다.

| 항목 | 기존 C6 문서 | 합이오 문서 | 이 통합본의 채택 기준 |
|---|---|---|---|
| 작업 구조 | G0~G6, T00~T16, 초기 설치·통신 검증 | G0~G7, 싱글 전투 후 연결 | **C6 순서 유지.** 설치·재설치 → 숫자 통신 → 전투 → 통합 |
| 최종 검증 인원 | 3인 고정 제안 | 2인 FIXED, 3~5인 제외 | **iPhone 2대 고정.** 3인 이상 검증으로 표현하지 않음 |
| 좌석·카메라 | 0/1/2, 0°/120°/240° | P1/P2, 같은 기본 구도 허용 | **P1=Host, P2=Client. 같은 기본 구도.** 기기별 각도는 후속 |
| 최초 생성 | 처음부터 1개씩 생성 | 첫 Tap에 5개, 비용 5 | **플레이어별 최초 5개 일괄 생성**, 이후 1개씩 |
| 음양 생성 | 성공한 생성마다 교대 | Host Seed 기반, Fixture 별도 | **Host Seed 기반.** 교대 규칙은 기본으로 유지하지 않음 |
| 스태미나 회복 | 시간 자동 회복 | 유효 피격 시 공격자만 +1 | **유효 피격 회복만.** 자동 회복을 몰래 추가하지 않음 |
| 오행 | 화/수/목 고정 및 재료 속성 | Generic 공격, 오행 계산 제외 | **음양과 Raw/Combined만 게임 규칙에 사용.** Element는 필요 시 예약 |
| 협동 비교 옵션 | 서로 다른 속성 조합 실험 | P1 음×5 / P2 양×5 Fixture | **기본 실행과 DEBUG_TEST_MODE Fixture를 분리.** 속성 강제 옵션은 제외 |
| 연결 수단 | 직접 IP만, 자동 탐색 제외 | 방 탐색 P0, IP Fallback P1 | **G2는 직접 IP로 위험 검증. T10-A에서 방 탐색 P0 추가.** IP 성공을 탐색 성공으로 보지 않음 |
| 전달 입력 | 좌우 경계 너머 드래그 | 수평 Swipe Threshold | **수평 거리·방향 우세 조건의 Swipe.** 속도 기반 추가 판정은 제외 |
| 공격 입력 | Attack Zone 최초 진입 | Zone 진입 또는 위쪽 Throw Threshold | **Attack Zone 최초 진입으로 통일.** 영역 밖에서의 독립 Throw Threshold는 후속 |
| 방어 | 변수만, 입력 제외 | Long Press 감지·상태 변수 | **변수만 P0, Long Press 감지·복제는 후속.** 합이오의 해당 입력 요구를 이번 통합본에서는 유보 |
| 공격 생명주기 | orbId 유지, 비행 후 종료 | 공격 절차에는 CONSUMED, 상태도에는 PROJECTILE | **2D 뷰 제거와 데이터 종료를 구분.** 같은 orbId로 PROJECTILE, 명중·만료 후 CONSUMED |
| 물리 | 실제 3D Rigidbody 충돌 | Rigidbody 또는 동등한 판정 | **실제 3D 비행·Collider 충돌 유지.** 타이머로 명중을 대체하지 않음 |
| 체력·수치 | HP 300, 피해 10, 보관 10 등 임시 | HP 100, 피해 20, 보관 20 등 임시 | **합이오 수치 채택.** 조정값은 중앙 설정과 변경 기록으로 관리 |
| 완성 구슬 보관 만료 | 이번 축소판 기본 비활성 제안 | 별도 보관 만료 규칙 없음 | **이번 통합본에서는 비활성 유지.** 3D 투사체 수명 3초와 구분 |
| 테스트·상태 | Task 검증과 PASS/FAIL/NOT_RUN/BLOCKED | AT-01~18, NOT TESTED 등 | **AT 번호 보존 + Task 매핑.** 미실행 표기는 NOT_RUN으로 통일 |
| 결과 문서 경로 | `docs/…` | `Docs/HAPIO_…` | **한 경로 체계로 통합.** 대소문자만 다른 중복 폴더를 만들지 않음 |

### 통합 시 드러난 검증 주의점

다음은 원문 규칙을 함께 읽었을 때의 **검토 메모**다. 이를 해결했다는 뜻도, 원문 규칙을 변경하라는 지시도 아니다.

**자원 고갈:** 합이오 규칙에서는 공격 구슬에 Raw 2개가 필요하지만 유효 피격으로 되돌아오는 스태미나는 1이다. 자동 회복도 없으므로 반복 공격이 무한히 유지되는 구조가 아니다. 빗나감이나 음양 편중으로 행동이 막힐 수 있다. 자동 회복·무료 구슬을 일반 플레이에 추가하지 말고 이 상황을 시험하고 기록한다. 기본 HP 100 / 피해 20에서는 유효 피격 5회가 승리에 필요하다.

**방 탐색:** 직접 IP 통신 성공과 자동 방 탐색 성공은 다른 검증이다. 탐색 구현이나 권한 설정이 막히면 해당 항목을 FAIL 또는 BLOCKED로 기록한다. IP Fallback이 동작해도 탐색 P0를 완료했다고 표시하지 않는다.

**협동 Fixture:** P1에 음만, P2에 양만 주는 시험은 전달 경로를 재현하기 위한 장치다. 그 결과로 일반 Host Seed 실행에서도 협동이 자연스럽게 발생한다고 단정하지 않는다.

**동시 이벤트 경계:** “같은 Tick”이라는 이유만으로 콜백 호출 순서에 승패를 맡기지 않는다. 통합본은 호스트의 충돌 처리 시각이 종료 시각보다 빠른지 확인한다. Team HP가 정확히 0인 시각의 공격은 승리를 만들지 않는다.

## 1.3 적용 우선순위와 기존 작업 이관

적용 순서는 최신 사용자 지시 → 이 통합본의 채택 PROJECT INPUT·결정표 → 해당 Task → Acceptance 기준 → 두 원문의 미충돌 보완 설명 → 기존 GDD·룰북·구현이다. Task나 테스트가 PROJECT INPUT과 달라지면 임의로 고치지 말고 충돌을 기록한다.

기존 프로젝트의 파일·에셋·검증 증거는 보존한다. 기존 사양이 통합본과 다르다는 이유로 프로젝트를 새로 만들거나 전체를 삭제하지 않는다. 기존 3인 구현을 이미 만들었다면 제거 범위와 재사용 범위를 기록하고, 2인 경로를 검증한다.

기존 G1 설치·재설치 증거는 당시 환경의 증거로 재사용할 수 있다. 하지만 규칙·씬·패키지가 바뀐 현재 빌드의 결과까지 자동 PASS로 승계하지 않는다. 3인·자동 회복 프리셋으로 실행했던 기능 시험은 새로운 2인·피격 회복 사양의 통과 근거가 아니다.

---

# 2. 통합 PROJECT INPUT

## 2.1 프로젝트와 목표

**PROJECT_NAME: 합이오 1차 MVP / C6**

실제 iPhone 두 대가 같은 Wi-Fi에서 연결된 상태로 다음 흐름이 성립하는지 검증한다.

```text
앱 실행 → 방 생성·탐색·참가 → P1/P2와 좌우 관계 확인 → Host Start
→ 각자 첫 Generate Tap으로 Raw 5개 생성
→ 필요 시 좌우 전달 → Yin + Yang 조합 → Combined Orb
→ Attack Zone으로 드래그 → 승인 후 2D 뷰 제거·3D 구슬 생성
→ 실제 3D 몬스터 충돌 → Monster HP 감소·공격자 Stamina 회복
→ Team HP 확인 → 반복 → 같은 Victory / Defeat → Reset 또는 Lobby
```

화면을 넘어 이어지는 전달감, 필요한 구슬을 요청·교환하는 협력감, 조합 결과가 몬스터 피격과 HP 변화로 이어지는 경험이 핵심이다. 완성형 게임의 콘텐츠·최종 디자인·장기 재미를 검증하는 문서는 아니다.

## 2.2 플랫폼·인원·연결

| 항목 | 적용 사양 |
|---|---|
| Unity·렌더 파이프라인 | 현재 프로젝트 우선 유지. 없을 때만 설치 환경을 확인해 새 프로젝트 생성 |
| 대상 | 실제 iPhone 2대, 한 사람당 한 기기, 세로 고정 |
| 실행 환경 | 같은 공간·같은 로컬 Wi-Fi, 게임 플레이에 인터넷 서비스 사용 안 함 |
| 목표 프레임률 | 60fps 목표. 지원 기기·실측 결과는 별도 기록 |
| 입력 | iPhone Touch 우선, Editor Mouse는 같은 행동 경로의 디버그 입력 |
| 화면 대응 | Safe Area, 다양한 iPhone 세로 비율, Home Indicator 여백 |
| Host / Client | 방 생성자는 Player 1 / Host, 참가자는 Player 2 / Client |
| 시작 | 두 명의 연결·초기 상태·Ready 확인 후 Host만 시작 |
| 이탈 | NETWORK_ERROR로 세션 중단. 자동 재접속·Host Migration 없음 |

2인에서는 왼쪽 이웃과 오른쪽 이웃이 모두 같은 상대다. **수신 대상은 같지만 진입 방향은 다르다.**

| 보낸 방향 | 상대 기기의 진입 위치 |
|---|---|
| Left | Right Edge 안쪽 |
| Right | Left Edge 안쪽 |

내부 좌석을 0/1로 저장할 수 있지만 UI에서는 P1/P2와 명확히 대응시킨다. 3인 선택지·빈 세 번째 슬롯·120° 카메라를 기본으로 만들지 않는다.

## 2.3 구현 우선순위와 제외 범위

**P0 — 필수:** iOS 빌드·기기 실행, 방 생성·탐색·참가, 플레이어 순서와 좌우 관계, Ready/Start, 초기 Raw 5개 생성, 추가 생성, 개인 스태미나와 피격 회복, 2D 드래그, 음양 조합·잘못된 조합 거부, Raw/Combined 좌우 전달, Attack Zone, 2D→3D 실제 공격, 일반 피격, HP·Clock·Result 동기화, 재시작·오류 안내, 최소 가독성 UI·진단, AT-01~AT-18.

**검증을 위해 먼저 만드는 보조 기능:** 직접 IP 연결·공유 숫자·개발용 구슬 공급. 합이오 원문의 IP Fallback은 P1이지만, C6 순서를 유지하기 위해 G2의 통신 검증 수단으로 앞당긴다. 최종 방 탐색 P0를 대체하지 않는다.

**P1 또는 후속:** 추가 실행 도우미, 세련된 피드백·Haptic·SFX, 방어 Long Press 감지·복제, 영역 밖 위쪽 Throw 입력, 자동 시험 확대. 지정된 Task에 없는 기능은 자동으로 추가하지 않는다.

**이번 범위 밖:** 몬스터 공격·AI·이동·특수 패턴, 실제 방어, 약점·급소·Critical, 오행 상성·피해 배율, 추가조합, 복수 몬스터·스테이지, 스토리·도감·성장·상점·재화·계정·랭킹·클라우드·백엔드, 3~5인, 기기별 몬스터 각도, 공유기 없는 연결, 연속된 공유 2D 물리·공동 잡기, Relay·온라인 매치메이킹·관전자·Host Migration·재접속, 정식 아트·VFX·애니메이션·음악·전체 접근성 옵션·스토어 제출, 장시간 운영 안정화·포괄적 치팅 방지 시스템.

소유자·요청 유효성 검사는 핵심 상태 일치를 위한 P0다. 포괄적 치팅 방지를 제외한다는 이유로 이 검사를 생략하지 않는다.

## 2.4 구슬 데이터와 생명주기

| 구분 | Raw Orb | Combined Orb |
|---|---|---|
| 음양 | Yin 또는 Yang | None |
| 조합 | 반대 음양 Raw와 가능 | 불가 |
| 전달 | 보관 중 가능 | 보관 중 가능 |
| 공격 | 불가 | 가능 |
| 피해 | 없음 | Generic 고정 피해 |

Element 필드는 향후 확장용으로 예약할 수 있지만, 이번 생성·조합·피해 조건에 오행을 넣지 않는다. 기존 코어·껍데기 형태는 표현에 재사용할 수 있다.

최소 데이터는 `OrbId`, `OrbKind`, `Polarity`, `OwnerPlayerId`, 권한 상태, 행동 시점의 `NormalizedPosition`, `EntrySide`, `SequenceNumber` 또는 동등한 중복 식별 정보다. 네트워크 요청에는 `sessionId`, `roundId`, `requestId`를 연결한다.

**종류·호스트 상태·로컬 조작 상태는 구분한다.** `Combined`는 구슬 종류다. `DRAGGING`·`Pending`은 로컬 입력/표현 상태로 둘 수 있다. 모든 손가락 이동을 전역 상태로 복제할 필요는 없다. 합이오 원문의 상태명을 모두 하나의 거대한 enum에 기계적으로 합치지 않는다.

```text
Raw: IDLE ↔ 로컬 DRAGGING
Raw + 반대 음양 Raw: 조합 승인 → 재료 2개 CONSUMED → 새 Combined IDLE
Raw / Combined: 전달 요청·잠금 → 승인 → 같은 ID, Receiver IDLE
Combined: 발사 요청·잠금 → LAUNCHING → PROJECTILE → CONSUMED
```

전달과 공격 전환은 **같은 OrbId**를 유지한다. 조합은 재료 ID 두 개를 종료하고 새 ID 하나를 만든다. 2D 오브젝트가 화면에서 제거됐다는 이유로 비행 중인 논리 구슬까지 유실·종료 처리하지 않는다. 명중 또는 수명 만료 후에 최종 종료한다.

## 2.5 생성과 스태미나

전투 시작 시 각 플레이어의 Stamina는 5다. 첫 Generate Tap은 Raw 5개를 일괄 생성하고 Stamina 5를 소비한다. 성공한 경우에만 `InitialBatchGenerated`를 기록한다. 이후 Tap은 Raw 하나당 Stamina 1을 소비한다.

초기 배치도 자원·보관 상한·상태·송신자·중복 요청 검증을 통과해야 한다. **5개 전체 성공 또는 전체 거부**로 처리하며, 거부 시 구슬 수·Stamina·최초 생성 여부가 바뀌지 않는다. 이 원자성 기준은 C6의 실패 시 자원 보존 원칙을 합이오의 배치 생성에 적용한 보강 사항이다.

초기 생성 전에 전달받은 구슬이 있을 수 있으므로 보관 상한 검사는 기존 보유 수까지 포함한다. 추가 생성 실패도 자원과 보유 구슬을 바꾸지 않는다.

음양은 호스트가 Seed 기반으로 결정한다. 일반 실행의 정확한 확률·난수 알고리즘은 원문에 명시되지 않았으므로 채택 방식을 `DEMO_ASSUMPTION`에 기록한다. 재현 시험용 Seed와 일반 실행을 구분하며, 거부 요청이 난수 진행에 미치는 정책도 명시해 테스트한다.

스태미나 회복은 **자신의 유효한 3D 피격당 1**, 최대 5다. 다른 사람의 공격, 중복 충돌, 빗나감, 만료, 전투 종료 후 충돌로 회복하지 않는다. 시간 자동 회복은 없다. 완성 구슬을 전달받아 공격했어도 실제 투척자가 회복한다.

DEBUG_TEST_MODE에서 P1 초기 음×5 / P2 초기 양×5 Fixture를 사용할 수 있다. 해당 모드임을 HUD·로그·보고에 표시하고 일반 생성 규칙으로 기록하지 않는다.

## 2.6 조합·전달·공격

**조합:** 같은 소유자에게 있는 사용 가능한 Raw Yin + Raw Yang만 성공한다. 같은 음양, 동일 ID를 두 재료로 사용, 다른 소유자, Raw+Combined, Combined+Combined는 거부한다. 승인 후 재료 두 개를 소비하고 중간 위치 등 기록된 기준에 새 Combined 하나를 만든다. 실패 시 재료를 유지한다.

**전달:** 보관 중이고 잠기지 않은 Raw/Combined만 가능하다. 호스트가 소유·연결·상대 보관 상한을 확인한 후 같은 ID의 소유자를 바꾼다. Sender 뷰 제거와 Receiver 반대쪽 경계 안쪽 배치를 확정 상태에 연결한다. 높이는 정규화 좌표로 최대한 보존한다. 실패 시 호스트의 확정 상태를 확인해 원래 구슬을 유지한다. 타임아웃만으로 복제·복구를 확정하지 않는다.

**공격:** Combined가 Attack Zone에 처음 들어오면 발사를 요청하고 조작을 잠근다. 승인 후 2D 뷰를 제거하고 같은 논리 구슬의 3D Projectile을 만든다. 호스트에서 실제 이동과 몬스터 Collider 충돌을 실행한다. 한 투사체는 피해와 공격자 회복을 각각 최대 한 번만 만든다. 미명중은 Lifetime 종료로 정리한다. Raw는 위로 보냈다는 이유로 삭제되거나 발사되지 않는다.

## 2.7 입력과 충돌 우선순위

초기값은 수평 이동량 0.18 화면 너비, 수평 방향 우세 비율 1.25, 조합 Drop 거리 0.08 화면 너비다. Threshold는 중앙 설정으로 관리한다. P0에서는 거리 기반 수평 Swipe를 사용하고 별도 속도 기반 플릭을 추가하지 않는다.

다음 중재 규칙은 두 원문의 중복 행동 방지 원칙을 하나로 연결하기 위한 **통합 구현 결정**이다.

1. UI에서 시작한 Touch는 구슬 행동으로 중복 처리하지 않는다. 한 Pointer는 한 구슬만 제어한다.
2. 구슬을 잡으면 해당 Pointer와 구슬을 연결한다. 추가 Pointer가 같은 구슬을 빼앗을 수 없다. 여러 구슬 동시 조작은 P0 필수가 아니며, 지원하지 않으면 두 번째 Pointer를 안전하게 무시한다.
3. 화면상 원본 Pointer 이동으로 Gesture/Zone을 검사한 뒤 로컬 표시를 Clamp한다. Clamp 때문에 전달·공격 입력을 감지할 수 없는 구조를 만들지 않는다.
4. Combined의 Attack Zone 진입 또는 수평 Swipe 조건 중 먼저 승인 요청을 만든 행동에 잠근다. 같은 입력 갱신에서 둘 다 충족하면 Attack Zone을 우선한다. Raw는 이 공격 우선 경로로 삭제되지 않는다.
5. 어느 전환 요청도 발생하지 않은 상태에서 Pointer Up을 받으면 Raw 조합 Drop을 검사한다. 단순 접촉만으로 조합하지 않는다.
6. 전달·조합·발사 요청 중인 구슬은 다른 행동에 재사용하지 않는다. 응답 누락은 호스트 조회로 해결하고, 확정할 수 없으면 오류 상태로 처리한다.
7. 수신 구슬은 경계 안쪽에 생성하고 새로운 사용자 제스처 전에는 자동 반송하지 않는다. 터치 취소·전투 종료·연결 종료 시 입력 소유를 정리한다.

합이오의 위쪽 Throw 거리 0.15 화면 높이·우세 비율 1.25는 **후속 입력 후보**로만 보관한다. 이번 P0에서 Zone에 들어오지 않은 구슬을 별도 Threshold로 발사하지 않는다. 방어는 `enableDefense=false`, 필요 시 `IsDefenseHeld=false` 같은 변수만 준비한다.

## 2.8 전투 상태와 승패

```text
BOOT → LOBBY → READY → PLAYING → VICTORY / DEFEAT → RESET 또는 LOBBY
연결 해제·복구 불가능한 오류 → NETWORK_ERROR
```

호스트가 전투 시작 시각, 종료 시각, roundId를 확정한다. Team HP는 시간형 공동 HP이며 PLAYING에서만 감소한다. Lobby·Ready·Result에서는 감소하지 않는다. 몬스터 공격에 의한 추가 감소는 없다.

유효 피해는 **호스트 충돌 처리 시각 < 종료 시각**일 때만 적용한다. 그 결과 Monster HP ≤ 0이고 Team HP > 0이면 Victory다. 종료 시각에 도달했거나 Team HP ≤ 0이면 Defeat다. 같은 시각에 둘 다 0이 되는 경우 Defeat다. 한 번 확정한 결과를 클라이언트 타이머나 늦은 이벤트가 바꾸지 못한다.

결과 진입 시 Clock·게임 입력을 멈추고 잔여 투사체의 추가 피해·회복을 차단한다. 재시작은 Host만 확정한다. 구슬·투사체·HP·Stamina·최초 생성 플래그·Pointer·Pending·디버그 Fixture 상태를 새 라운드 규칙에 맞춰 초기화한다. roundId를 갱신하고 이전 요청은 거부한다.

연결 이탈은 Victory/Defeat와 별도다. 어느 한쪽이라도 이탈하면 세션을 중단하고 NETWORK_ERROR를 표시한다. 자동 재접속·Host Migration·진행 중 이어하기는 하지 않는다.

## 2.9 중앙 설정

`GameConfig`, `PrototypeConfig` 또는 기존 동등한 Asset **한 곳**을 원본으로 사용한다. 아래 두 이름을 이유 없이 함께 만들지 않는다.

| 설정 | 채택값 | 성격 |
|---|---:|---|
| MVP Player Count | 2 | FIXED |
| Initial Raw Orb Count | 5 / player | FIXED |
| Max Stamina | 5 | FIXED |
| Initial Stamina | 5 | 원문 DEMO_TUNING_VALUE, 최초 비용·개수와 함께 검증 |
| Initial Batch Cost | 5 | DEMO_TUNING_VALUE |
| Regular Spawn Cost | 1 | DEMO_TUNING_VALUE |
| Recovery Per Valid Hit | 1 | DEMO_TUNING_VALUE |
| Timed Stamina Recovery | Disabled | 채택 규칙 |
| Monster Max HP | 100 | DEMO_TUNING_VALUE |
| Base Damage | 20 | DEMO_TUNING_VALUE |
| Team HP / Battle Duration | 180 seconds | DEMO_TUNING_VALUE |
| Team HP Decay | 1 / second | Host Clock 기준 |
| Max 2D Orbs Per Device | 20 | DEMO_TUNING_VALUE |
| Horizontal Swipe Distance | 0.18 screen width | DEMO_TUNING_VALUE |
| Horizontal Direction Ratio | 1.25 | DEMO_TUNING_VALUE |
| Combination Radius | 0.08 screen width | DEMO_TUNING_VALUE |
| Attack Trigger | Attack Zone first entry | 채택 규칙 |
| Projectile Speed | 12 units / second | DEMO_TUNING_VALUE, 장면 크기에 맞춰 검증 |
| Projectile Lifetime | 3 seconds | DEMO_TUNING_VALUE |
| Upper / Lower View | 55% / 45% | C6 임시 화면 설정 승계 |
| Target FPS | 60 | 목표, 실측 보장 아님 |
| Network Tick / Snapshot | 20 Hz | DEMO_TUNING_VALUE, 이벤트 신뢰성과 구분 |
| Stored Combined Orb Expiry | Disabled | C6 축소 범위 승계, 투사체 수명과 구분 |
| Defense Input / Resolution | Disabled | 변수만 예약 |

Fixture와 일반 Host Seed 실행은 명시적으로 구분한다. 설정 변경 시 값·이유·수정 위치·영향받는 테스트를 기록한다. 기본 프리셋을 바꿔놓고 원래 수치로 통과했다고 보고하지 않는다.

## 2.10 물리·카메라·UI·에셋

하단 2D 구슬은 Touch Drag와 Drop 판정이 가능해야 한다. 구슬끼리 물리적으로 밀리고 튀는 기능은 P0가 아니다. 표시 위치는 로컬에서 관리하고 전달·조합·발사 시 필요한 정규화 좌표만 요청에 포함한다.

상단에는 고정된 3D 허수아비와 실제 Projectile을 표시한다. 몬스터는 단순 Hit Collider를 사용하며 AI·이동·공격·반동이 없다. 투사체끼리의 충돌을 막고 빠른 투사체 관통·한 번 피해·수명 종료를 검증한다.

기존 URP 프로젝트에서는 Universal Renderer의 Base Camera 두 개로 상단/하단을 나누는 C6 구성을 우선 검토한다. 상단은 3D, 하단은 Orthographic 2D, HUD는 Screen Space - Overlay다. 기존 렌더 파이프라인을 무단 교체하지 않는다. RenderTexture·카메라 스태킹은 별도 필요가 확인되지 않으면 추가하지 않는다.

두 기기는 같은 기본 몬스터 구도를 사용한다. 각 기기는 공통 전장을 로컬 렌더링하며 영상 스트리밍을 하지 않는다. 2D→3D 경계의 위치·크기·이동 방향은 실기기에서 연결감을 확인한다. 발사자는 데이터로 기록하고, Host 화면 카메라 객체에 다른 플레이어의 판정을 결합하지 않는다.

| 화면 | 필수 표시 |
|---|---|
| Lobby | MVP명, Create, Room 목록/Join, IP Fallback, Host/Client, P1/P2 연결, Local Player, Left/Right 상대, Ready, Host Start, 오류 |
| Battle | Monster HP, Team HP/남은 시간, Local Stamina, Generate, Local Player, Network 상태, Left/Right Boundary, Attack Zone |
| Result | Victory/Defeat, 최종 HP·시간, Reset/Retry, Lobby 복귀 |
| Debug | 빌드·역할·연결·입력·구슬·행동·HP·자원·충돌 확인 정보. 필요할 때 펼쳐 표시 |

Yin/ Yang, Raw/Combined, 전달 경계, Attack Zone, 피격 성공, 결과 상태는 즉시 구분 가능하게 한다. 최종 아트보다 가독성과 최소 동작 피드백을 우선한다. 핵심 HUD는 Safe Area 안에 두고 Home Indicator와 드래그 영역이 겹치지 않게 한다. 장식 UI는 입력을 가로채지 않으며 불투명 HUD가 공격 구간을 가리지 않게 한다.

필수 외부 Asset과 P0 Audio는 없다. Primitive·기본 Material·단순 Sprite·프로젝트에서 사용 가능한 Font를 사용한다. 아트 부재를 Blocker로 취급하지 않는다. 최종 SFX·Haptic은 후속이다.

## 2.11 네트워크와 최소 책임 분리

호스트는 플레이어 순서·구슬 ID·생성·조합·전달·발사·충돌·피해·Stamina·Clock·승패를 확정한다. 클라이언트는 입력, 로컬 드래그 피드백, 요청, 확정 상태 렌더링과 HUD를 담당한다. Host 플레이어도 동일한 검증 함수를 거친다.

기존 패키지를 우선 재사용한다. 없다면 설치 Unity와 호환되는 Netcode for GameObjects + Unity Transport 또는 동등한 방식의 선택 근거를 기록한다. API와 실제 iOS 제약은 작업 시 설치 버전의 공식 자료로 확인한다.

중요 생성·조합·전달·발사·피해·상태·결과는 신뢰성 있는 확정 전달을 사용한다. 필요할 때만 투사체 표시용 Transform 보간·Snapshot을 추가한다. 2D 드래그 좌표를 매 Frame 양쪽에 복제하지 않는다.

최초 상태 및 재확인용 Snapshot에는 세션·라운드·revision, 플레이어·Ready, 구슬 메타데이터·소유자, 개인 자원·초기 생성 여부, HP·종료 시각·결과를 포함한다. 같은 revision의 논리 상태를 비교하며 화면 픽셀이나 표시 타이머의 순간 오차를 데이터 불일치로 오인하지 않는다.

방 탐색은 설치 환경에 적합한 LAN Discovery/Bonjour 등 구현을 선택한다. UDP Broadcast·Bonjour 등 이름만 적어 구현했다고 하지 않는다. 실제 탐색·선택·참가와 필요한 iOS 선언·권한을 시험한다. 자동 탐색 실패 시 직접 IP Fallback은 원인 분리용으로 남긴다.

필요 책임은 Config, Session, PlayerTopology, BattleState, OrbRegistry, Spawn/Input/Combine/Transfer, Attack/Projectile/Monster, TeamHP/Stamina, HUD/Lobby/SafeArea, Debug/Tests다. 반드시 각각 별도 클래스일 필요는 없다. 하나의 거대 Script나 불필요한 DI·Factory·플러그인 프레임워크 양쪽을 피한다.

권장 신규 경로는 `Assets/_Project/HapioMVP/` 아래 Scenes, Scripts, Prefabs, Config, Materials, Tests/EditMode, Tests/PlayMode, Editor다. 기존 폴더 규칙이 있으면 우선 유지한다.


---

# 3. Requirement Gate와 작업 역할

## 3.1 역할

Codex는 Unity Gameplay Engineer, Technical Lead, Technical Game Designer, iOS Build/Test Engineer, Multiplayer Prototype Engineer의 관점에서 지정 Task를 구현·시험한다. 도구가 있다면 코드 예시만 제시하지 말고 실제 Script·Scene·Prefab·참조·설정을 연결한다.

Unity MCP가 실제 연결돼 있고 필요한 동작을 지원하면 사용한다. 없으면 파일 시스템, 사용 가능한 Unity CLI/Editor 도구를 사용한다. 도구를 사용할 수 있다고 가정하거나 실행 결과를 만들어내지 않는다. Scene/Prefab 직접 편집이 불안정하면 반복 실행 가능한 Editor Setup Script를 제공한다.

사람은 Apple 계정·Signing·기기 신뢰·Developer Mode·실제 손가락 입력·플레이 관찰 등 Codex가 실제 수행할 수 없는 항목을 담당한다. 코드를 만들었다는 사실과 사람이 기기에서 확인했다는 사실을 구분한다.

## 3.2 요구사항 검사

구현 전 해당 Task의 요구사항을 다음 기준으로 확인한다.

- CLEAR: 하나의 동작으로 해석할 수 있는가?
- IMPLEMENTABLE: 무엇을 연결·구현해야 하는가?
- TESTABLE: 성공·실패를 어떤 실행으로 판단하는가?
- MVP-RELEVANT: 이번 검증에 필요한가?

MVP Goal, Success Criteria, Core Experience, Core Loop, Input, P0, Rules, Player/Device, Network, Win/Lose, Scenario, Acceptance를 점검한다. 합이오 원문의 `BLOCKER COUNT: 0`은 예상값이었으므로 그대로 복사하지 않는다. 실제 저장소·환경·현재 Task의 조건으로 결과를 작성한다.

내부 폴더명·클래스명·Collider·Rigidbody 세부값·설정 Asset 종류·패키지 내부 설계는 근거를 기록하고 결정한다. 이 통합본에서 해결한 2인·초기 생성·회복·승패·방어 범위를 다시 질문하지 않는다. 미정 튜닝값은 `DEMO_TUNING_VALUE` 또는 `DEMO_ASSUMPTION`으로 기록한다.

새로 발견된 요구사항 충돌이 플레이 결과를 바꾸면 관련 동작을 임의 구현하지 않는다. 기존 문서·코드로 먼저 확인하고 영향 범위와 미정 항목을 기록한다. 필요한 확인은 해당 결정으로 제한하며, 이미 답이 있는 일반 질문 목록을 다시 보내지 않는다.

환경 문제는 영향받는 단계만 BLOCKED로 표시한다. 예를 들어 Signing에 접근할 수 없어도 현재 Task의 Script·Setup Tool·자동 시험을 준비할 수 있다. 다만 **필수 실기기 Gate를 통과하지 않은 상태로 다음 G단계 기능을 만드는 것은 허용하지 않는다.**

## 3.3 현재 프로젝트 점검

프로젝트 경로·Git 변경, Unity 버전·실행 경로·iOS 모듈, 기존 Scene·Build Scene·GameObject·Script·Prefab·Asset, 입력·렌더·물리·네트워크 패키지, Platform·Player Settings·Bundle ID·Portrait·FPS·Safe Area, Console Error/Warning, macOS·Xcode·선택 도구 경로·SDK를 확인한다.

기존 시스템이 있으면 재사용하고, 기존 Warning과 새 Warning, 진행을 막는 항목과 그렇지 않은 항목을 나눈다. 실제 Console 또는 빌드 로그에 접근하지 못했다면 정적 검토라고 표시한다. 접근 불가능한 기기·Team ID·인증서를 임의로 채우지 않는다.

---

# 4. Codex 공통 작업 지시 — 최초 적용 및 매 Task 재확인

```text
현재 프로젝트에서 “합이오 1차 MVP”를 Unity로 개발해 iOS에서 검증한다.
C6_Hapio_Codex_Integrated_Master_Prompt.md의 채택 사양을 기준으로
지정된 Task 하나만 수행한다. T10은 A/B를 각각 별도 요청으로 실행한다.
다음 Task 또는 전체 MVP를 자동으로 구현하지 마.

[작업 시작]
- 기존 AGENTS.md, 하위 지시, Git 상태, PROJECT INPUT,
  docs/PROTOTYPE_SPEC.md, docs/VALIDATION.md를 읽어라.
- 기존 지시와 사용자 변경을 보존하고 충돌 없는 공통 규칙을 병합해라.
- 이번 통합본과 기존 구현의 차이를 먼저 기록해라.
- 이전 대화를 기억한다고 가정하지 말고 저장소 문서를 다시 읽어라.
- 선행 Task와 실기기 Gate 증거를 확인해라.

[이번 채택 범위]
- 실제 iPhone 2대, 같은 Wi-Fi, P1 Host / P2 Client다.
- 3인 이상과 플레이어별 120° 카메라는 이번 범위가 아니다.
- 몬스터는 고정된 3D 허수아비다. 공격·약점·AI·반동은 없다.
- 방어는 비활성 변수만 둔다. Long Press 입력·연출·판정은 만들지 않는다.
- 첫 Generate Tap은 Raw 5개와 Stamina 비용 5다.
- 이후 생성은 Raw 1개와 비용 1이다.
- 음양은 Host Seed 기반이다. 일반 실행과 고정 Fixture를 구분한다.
- Yin + Yang만 조합한다. 오행 상성·피해 배율·추가조합은 없다.
- 스태미나는 유효한 3D 피격 시 실제 공격자만 1 회복한다.
  시간 자동 회복, 무료 보충, 빗나감 회복을 임의 추가하지 않는다.
- 완성 구슬은 Attack Zone 최초 진입 시 요청하고 실제 3D 공격으로 전환한다.
- 시간형 Team HP, Monster HP, 승패·재시작·연결 오류를 구현한다.
- G2는 직접 IP 연결 시험, T10-A는 자동 Room Discovery P0다.
  IP 성공을 Discovery 성공으로 보고하지 않는다.
- 모든 값은 하나의 Config 원본에 두고 변경 이유를 기록한다.

[구슬과 호스트 권한]
- 호스트가 생성·조합·전달·발사·피격·자원·시간·승패를 최종 확정한다.
- 호스트 플레이어도 같은 게임 요청 검증 함수를 사용한다.
- 실제 통신 송신자를 기준으로 좌석을 검사한다.
  클라이언트가 임의로 보낸 playerId만 신뢰하지 않는다.
- sessionId/roundId/requestId와 필요한 sequence/revision으로
  중복·이전 요청을 구분한다. 중복 요청을 두 번 적용하지 않는다.
- 생성은 성공할 때만 비용을 소비한다. 초기 배치도 전체 성공/전체 거부다.
- 전달·2D→3D 전환은 같은 orbId, 조합은 재료 둘 종료·새 ID 하나다.
- 2D 뷰 제거와 논리 구슬의 최종 CONSUMED를 구분한다.
- 피해와 스태미나 회복은 같은 유효 피격에 대해 각각 한 번만 적용한다.
- hit 알림을 받은 클라이언트가 HP나 회복을 다시 적용하지 않는다.
- 로컬 2D 드래그는 표시다. 매 프레임 전체 공유 물리로 확장하지 않는다.
- Pending은 입력 잠금이다. 응답 지연만으로 원본을 복제하거나 되살리지 않는다.
  호스트 확정 상태를 재확인하고, 확인 불능이면 오류로 처리한다.
- 어느 한쪽 이탈 시 세션을 중단한다. 자동 재접속·Host Migration은 없다.

[입력·화면]
- 실제 Touch 우선, Editor Mouse는 같은 행동 경로를 사용한다.
- UI 입력·구슬 입력 중복, 같은 구슬 이중 잡기, 전환 중 재사용을 막는다.
- 전달은 수평 Swipe 거리·방향 우세, 공격은 Zone 진입, 조합은 Drop이다.
- 2.7의 행동 중재와 취소 규칙을 따른다.
- 2D 구슬끼리 밀어내기, 두 화면 공동 잡기는 P0가 아니다.
- 상단 3D·하단 2D·고정 HUD 구성을 유지한다.
- 두 기기는 같은 기본 몬스터 구도를 사용하며 각 기기에서 렌더링한다.
- Safe Area, Home Indicator, 버튼 크기, 장식 UI의 입력 차단을 확인한다.
- Primitive와 기본 Asset을 사용한다. 최종 아트·사운드는 추가하지 않는다.

[환경과 작업 안전]
- ProjectVersion.txt, manifest.json, packages-lock.json,
  실제 Xcode 환경과 기존 설정을 확인한다.
- Unity·렌더러·패키지를 임의 업그레이드·교체하지 않는다.
- 필요한 패키지는 해당 Task에서만 추가하고 실제 호환성과 버전을 기록한다.
- 설치 버전의 공식 API를 확인한다. 최신 예제를 추측해 복사하지 않는다.
- Unity MCP 등 도구는 실제 연결·권한·기능을 확인한 경우에만 사용한다.
- Script만 만들지 말고 Scene·Prefab·참조까지 연결하거나
  반복 실행 가능한 Editor Setup Tool과 정확한 실행 절차를 제공한다.
- 반복 실행 시 Camera·Canvas·NetworkManager·구독이 중복되지 않게 한다.
- 같은 프로젝트가 Unity Editor에 열려 있으면 별도 batchmode를 강행하지 않는다.
  프로세스 강제 종료, lock 삭제, 전체 Library 삭제를 자동 수행하지 않는다.
- 비밀번호·개인 키·서명 인증서를 저장소에 넣지 않는다.
- Team ID·Certificate·Profile·실제 배포용 Bundle ID를 꾸며내지 않는다.
- Xcode 수동 수정만 설정의 유일한 원본으로 남기지 않는다.
- 실제 산출물의 Info.plist와 설정을 확인한다.
- .meta·기존 Scene·사용자 변경을 임의 삭제하거나 되돌리지 않는다.
- App Store/TestFlight 업로드, 유료 서비스, Git push는 범위 밖이다.
- 기존 docs 또는 Docs 경로를 확인해 대소문자 중복 폴더를 만들지 않는다.

[검증]
- 현재 Task의 관련 자동 시험을 작성하고 실행 가능한 범위에서 실행한다.
- Compilation Error가 남으면 다음 기능으로 진행하지 않는다.
- Warning은 기존/신규와 영향도를 구분한다.
- 종료 코드뿐 아니라 XML 결과·실행 개수·실패·누락을 확인한다.
- 테스트 0개, 타임아웃, 미실행을 PASS로 취급하지 않는다.
- Script 존재, Component 부착, Prefab 생성만으로 동작 완료라고 하지 않는다.
  Input → Request/Logic → State → 실제 표시가 연결됐는지 확인한다.
- Compile, EditMode, PlayMode, Host/Client,
  Xcode 프로젝트 생성, 앱 빌드·서명, 실제 iPhone 실행을 분리한다.
- 결과는 PASS / FAIL / NOT_RUN / BLOCKED로 통일한다.
- 미실행 사유로 DEVICE TEST NOT PERFORMED,
  BUILD GENERATED / DEVICE RUN NOT TESTED,
  MANUAL STEP REQUIRED를 부가 기록할 수 있다.
- 기존 원문의 NOT TESTED는 NOT_RUN으로 옮기되 과거 원문 결과는 보존한다.
- 사람의 기기 증거 없이 실기기 항목을 PASS로 바꾸지 않는다.
- 로그는 빌드·Config·Fixture·기기·실행일·Task·AT와 연결한다.
- Debug 주입·강제 결과·단축 시간 시험을 실제 일반 플레이와 구분한다.
- 상태 비교는 같은 roundId/revision의 논리 값으로 한다.
- 로그에 비밀정보·불필요한 개인/네트워크 식별정보를 남기지 않는다.

[매 Task 완료 보고]
변경 파일, 구현 동작, 실제 실행한 시험, 결과와 근거,
미실행 항목, 사람의 확인 순서, 남은 문제,
다음 Task 진행 가능 여부를 한국어로 보고한다.
docs/VALIDATION.md와 해당 AT 결과를 갱신한다.
현재 Task의 실행 완료와 전체 MVP 완료를 구분한다.
다음 Task는 자동 시작하지 마.
```

---

# 5. G0~G6 / T00~T16 실행 순서

## 5.1 Task 목록

기존 C6의 번호와 진행 순서를 유지한다. 합이오의 별도 G0~G7 번호는 이 파일에서 실행 번호로 사용하지 않는다.

| Task | 단계 | 작업 | 선행 조건 | 실기기·사람 확인 |
|---|---|---|---|---|
| T00 | G0 | 환경·요구사항·통합 차이 점검 | 없음 | 환경·범위 확인 |
| T01 | G1 | 최소 앱 설치·재설치 | T00 | iPhone Build A → 수정 B |
| T02 | G2 | 직접 IP 호스트·참가 | T01 + G1 PASS | T03과 함께 두 기기 |
| T03 | G2 | 호스트 권위 숫자 동기화 | T02 | 두 iPhone 100개 고유 요청 |
| T04 | G3 | 상단 3D·하단 2D·HUD 골격 | T03 + G2 PASS | T06에서 화면 확인 |
| T05 | G3 | 구슬 모델·Touch·Gesture 중재 | T04 | T06에서 실제 입력 |
| T06 | G3 | 2D→3D 전환·실제 피격 | T05 | 직접 드래그 20회 |
| T07 | G4 | 초기 5개·추가 생성·개인 자원 | T06 + G3 PASS | T09에서 비용·회복 |
| T08 | G4 | 음양 1단계 조합 | T07 | T09에서 성공·거부 |
| T09 | G4 | Team Clock·승패·Reset | T08 | 한 iPhone Core Loop |
| T10-A | G5 | 2인 Lobby·방 탐색·Ready | T09 + G4 PASS | T12에서 자동 탐색 |
| T10-B | G5 | 기존 게임 상태의 2인 동기화 | T10-A 구현·코드 검증 | T12에서 실기기 통합 |
| T11 | G5 | 좌우 Swipe 전달 | T10-B | T12에서 전달 100회 |
| T12 | G5 | 같은 기본 구도·2인 전체 루프 | T11 | iPhone 2대, AT-01~18 |
| T13 | G6 | 앱 중단·연결 실패 | T12 + G5 PASS | 실제 중단·오류 |
| T14 | G6 | 무결성·진단·자동 시험 | T13 | 실기기와 대조할 근거 |
| T15 | G6 | 반복·성능·화면 QA | T14 | 3분 시험 5회, 두 화면 크기 |
| T16 | G6 | 협동 관찰·최종 판단 | T15의 실행/미실행 기록 | 일반 Seed와 Fixture 분리 |

T10-A의 실기기 탐색이 아직 미검증이어도 동일 G5 내 T10-B의 코드 연결 작업은 가능하다. 그러나 T10-A가 구현·컴파일 차원에서 막혔다면 해당 차단을 해결하기 전 통합 완료로 처리하지 않는다. 최종 G5에서는 자동 탐색을 포함한 실제 두 iPhone 검증이 필요하다.

## 5.2 Gate의 의미

| Gate | 통과 근거 |
|---|---|
| G0 | 요구사항·환경·미설정·현재 Task가 구분된 기록. 게임 동작 PASS는 아님 |
| G1 | Unity 수정이 반영된 Build B가 실제 iPhone에서 실행됨 |
| G2 | 직접 IP로 연결한 두 iPhone에서 100개 요청과 승인 수·상태 일치, 실패 재시도 확인 |
| G3 | iPhone에서 20회 직접 드래그한 2D→3D 전환·실제 충돌·한 번 피해·만료 확인 |
| G4 | 한 기기의 명시적 개발 모드에서 초기/추가 생성·조합·피격 회복·Clock·승패·Reset 확인 |
| G5 | 자동 방 탐색·참가 포함 iPhone 2대 Core Loop와 AT-01~18, 좌우 전달 100회 |
| G6 | 중단·반복·기기별 QA와 실제 관찰 결과 기록. 기술 판정과 협동 판정을 별도로 작성 |

100회·20회·5회는 기존 C6가 제안한 검증 횟수이며 공식 안정성 기준이 아니다. 현재 요청은 문서 통합이므로 이 파일은 어떤 Gate도 실제로 PASS 처리하지 않는다.

G6의 기술 항목이 통과했다고 협동의 재미를 PASS로 처리하지 않는다. 반대로 협동 관찰 결과가 미약하더라도 확인한 통신·물리 실행 사실까지 지우지 않는다.

## 5.3 AT와 주 구현 Task

| AT | 요구사항 | 구현 Task | 최종 기기 검증 |
|---|---|---|---|
| AT-01 | 방 생성 | T02, T10-A | T12 |
| AT-02 | 발견한 방 참가 | T10-A | T12 |
| AT-03 | P1/P2·좌우 관계 | T10-A | T12 |
| AT-04 | 최초 Raw 5개 | T07 | T12 |
| AT-05 | 올바른 음양 조합 | T08 | T12 |
| AT-06 | 같은 음양 거부 | T08 | T12 |
| AT-07 | 왼쪽 전달 | T11 | T12 |
| AT-08 | 오른쪽 전달 | T11 | T12 |
| AT-09 | 공격 시작·중복 방지 | T06 | T12 |
| AT-10 | 3D 전환 | T06 | T12 |
| AT-11 | 실제 피격·한 번 피해 | T06, T07 | T12 |
| AT-12 | Team HP 감소 | T09, T10-B | T12 |
| AT-13 | Victory | T09, T10-B | T12 |
| AT-14 | Defeat | T09, T10-B | T12 |
| AT-15 | 추가 생성 비용 | T07 | T12 |
| AT-16 | 공격자만 회복 | T07, T10-B | T12 |
| AT-17 | Raw 공격 거부 | T05, T06 | T12 |
| AT-18 | 중복·상태 일치 | T10-B, T11, T14 | T12, T15 회귀 확인 |

---

# 6. Task별 실행 프롬프트

각 프롬프트는 2장의 PROJECT INPUT과 4장의 공통 지시를 함께 적용한다. 해당 Task만 실행하고 결과를 남긴다.


## T00 · G0 · 환경·요구사항·통합 차이 점검

선행: 없음  
사람·실기기 확인: 현재 환경과 적용 범위 확인

```text
현재 Task는 T00 / G0다. 공통 지시와 통합 결정표를 먼저 읽어라.
기존 Unity 프로젝트와 사용자 변경을 보존해라.

[작업]
1. 3.2의 CLEAR/IMPLEMENTABLE/TESTABLE/MVP-RELEVANT 기준으로
   12개 요구 영역의 Readiness를 실제 확인한다.
   원문 템플릿의 READY·BLOCKER 0을 그대로 복사하지 않는다.
2. Unity 프로젝트·경로·버전·실행 도구·iOS 모듈·씬·Asset·입력·물리·렌더·패키지를 확인한다.
3. macOS·Xcode·선택 도구 경로·SDK와 현재 공식 호환성을 접근 가능한 범위에서 확인한다.
4. 기존 3인·자동 회복·속성 생성 등의 구현이 있다면
   이번 2인·초기 배치·피격 회복 사양과의 차이를 기록한다.
5. AGENTS.md, docs/PROTOTYPE_SPEC.md, docs/VALIDATION.md,
   docs/IOS_RUNBOOK.md를 생성하거나 안전하게 병합한다.
   canonical docs 경로와 원문 보고 파일의 대응을 기록한다.
6. 초기 생성·회복 규칙에서 자원 고갈 가능성은 검증 위험으로 기록한다.
   임의로 자동 회복이나 무료 공급을 추가하지 않는다.
7. 프로젝트가 없으면 기존 폴더를 덮어쓰지 않는다.
   설치된 Editor로 별도 생성 가능한지 확인하고, 불가능하면 정확한 수동 작업을 적는다.
8. 이번에는 게임 기능·패키지 설치·버전 교체를 하지 않는다.

[완료 기준]
확인된 환경, 미정 튜닝값, 실제 환경 Blocker,
요구사항 충돌과 사람의 작업이 구분된다.
T01의 대상 프로젝트·테스트 씬 위치·빌드 출력 경로가 명확하다.
실행하지 않은 빌드·기기 시험은 NOT_RUN이다.
T01을 자동 시작하지 마.
```


## T01 · G1 · 최소 앱의 iPhone 설치와 재설치

선행: T00  
실기기/사람 확인: 실제 iPhone 설치·수정 버전 재설치

```text
현재 Task는 T01 / G1: 최소 앱 설치·재설치 검증이다.
AGENTS.md와 프로젝트 사양·검증 기록을 읽고 T00의 차단 항목부터 확인해라.

[구현]
1. 빌드 ID, 버튼, 클릭 횟수만 있는 별도 테스트 씬을 만든다.
2. 실제 프로젝트 버전에 맞는 iOS 설정과 Xcode 프로젝트 생성 경로를 준비한다.
3. 테스트 실행과 iOS 프로젝트 생성의 반복 명령 또는 Editor 메뉴를 만든다.
   명령 인자·경로는 현재 환경에서 확인하고 절대 경로를 소스에 무분별하게 고정하지 않는다.
4. 빌드 로그와 테스트 XML을 보관하고 실패 시 성공처럼 종료하지 않는다.
5. 생성한 Xcode 프로젝트를 실제로 찾고, 접근 가능한 환경에서 빌드를 시도한다.
   미서명 컴파일과 서명된 기기용 앱을 구분한다.
6. 계정 로그인·Team·개발자 모드·기기 연결 등 사람 작업을 구체적으로 정리한다.
7. 첫 버전 A 실행 후 문구를 B로 바꿔 Unity 재빌드→재설치하는 절차를 준비한다.

[제외]
네트워크, 구슬, 몬스터, 정식 UI, 스토어 배포는 만들지 않는다.

[완료 기준]
Unity 컴파일→Xcode 프로젝트 생성→앱 빌드·서명→iPhone A 실행→B 재설치가 구분돼 기록된다.
실제 iPhone에서 B가 실행된 증거가 있어야 G1 PASS다.
여기서는 코드가 완성됐어도 기기 확인이 없으면 G1은 NOT_RUN/BLOCKED로 남긴다.
T02를 자동 시작하지 마.
```


## T02 · G2 · 직접 IP 호스트·참가 연결

선행: T01 및 G1 PASS  
사람·실기기 확인: T03과 함께 iPhone 두 대

```text
현재 Task는 T02 / G2: 직접 IP로 통신 경로를 먼저 검증한다.
G1의 실기기 설치·수정 버전 실행 증거를 확인해라.
이번 작업은 최종 Room Discovery가 아니라 기반 연결 시험이다.

[구현]
1. 기존 통신을 재사용할 수 있는지 확인한다.
   없으면 설치 Unity와 호환되는 Netcode for GameObjects + Unity Transport
   또는 동등한 방식의 선택 이유와 실제 패키지 버전을 기록한다.
2. 호스트 시작, IP·포트 입력, Join, 취소·종료 UI를 만든다.
   호스트 주소를 확인할 최소 방법도 제공한다.
3. 연결 전/중/성공/실패, 역할, 참가자 수를 표시한다.
4. 잘못된 주소, 시간 초과, 버튼 중복, 종료 후 수동 재시도를 처리한다.
5. NSLocalNetworkUsageDescription을 재현 가능한 빌드 후처리로 적용하고
   실제 생성 Info.plist를 확인한다.
   아직 쓰지 않는 Bonjour·Broadcast 설정을 이유 없이 추가하지 않는다.
6. 원인이 확인되지 않은 실패를 “권한 거부”로 단정하지 않는다.
   주소·동일 Wi-Fi·네트워크 격리·권한 확인을 구분해 안내한다.
7. 호스트를 포함한 두 참가자를 식별한다.
   최종 P1/P2 Lobby와 자동 탐색은 T10-A에서 구성한다.

[검증]
두 인스턴스 연결·종료·새 연결과 중복 NetworkManager/구독 부재를 확인한다.
실기기 두 대 확인 순서를 만든다. 실제 미실행이면 NOT_RUN으로 둔다.
IP 직접 연결 성공은 AT-02 자동 방 탐색의 PASS가 아니다.

게임 동기화·구슬·Relay·계정·자동 방 검색은 이번 Task에 추가하지 마.
```


## T03 · G2 · 호스트 권위 숫자 동기화

선행: T02  
실기기/사람 확인: 두 iPhone 총 100회 요청

```text
현재 Task는 T03 / G2: 공유 숫자로 요청·승인·동기화를 검증한다.
T02의 연결을 재사용하고 AGENTS.md의 보고 규칙을 적용해라.

[구현]
1. 두 기기 모두에 +1 버튼과 공유 숫자를 표시한다.
2. 버튼은 요청만 보내며 숫자는 호스트만 변경한다.
   호스트 플레이어도 동일한 검증 함수를 거친다.
3. 송신자, sessionId, requestId를 사용해 같은 요청의 재수신을 중복 적용하지 않는다.
   중복이면 기존 처리 결과를 반환하고, 처리 기록은 세션 종료 시 정리한다.
4. 요청 발송 수, 호스트 승인 수, 거부 수, 상태 revision을 표시한다.
5. 초기 연결 시 전체 상태를 받아 현재 숫자를 맞춘다.
6. 자동 시험은 순차·동시·중복 요청을 검증한다.
   실제 기기 입력과 자동 시험의 결과는 따로 기록한다.

[실기기 gate]
같은 Wi-Fi의 iPhone 2대에서 총 100개의 고유 요청을 처리한다.
응답을 확인한 뒤 양쪽 값=호스트 승인 수가 되는지 확인한다.
권한 첫 요청, 연결 실패 후 수동 재시도도 확인한다.
연결 앱이 실행됐다는 사실만으로 숫자 일치를 PASS 처리하지 않는다.

[제외]
구슬, 전투, 범용 메시지 프레임워크를 만들지 않는다.
G2 실기기 결과를 VALIDATION에 남기고 T04는 자동 시작하지 마.
```


## T04 · G3 · 상단 3D·하단 2D·고정 UI

선행: T03 및 G2 PASS  
실기기/사람 확인: T06에서 화면·입력 실기기 확인

```text
현재 Task는 T04 / G3: 전투 프로토타입의 화면만 구성한다.
기존 프로젝트와 G2 연결 코드를 보존하고 공통 규칙을 적용해라.

[구현]
1. 상단은 3D 허수아비와 배경, 하단은 2D 구슬 조작 영역으로 나눈다.
2. URP 프로젝트라면 Universal Renderer의 Base Camera 두 개와 Viewport Rect를 우선 검토한다.
   기존 렌더 파이프라인은 무단 교체하지 말고 다른 구성이라면 같은 화면 목표를 달성해라.
3. 전투/구슬/배경 레이어와 카메라 Culling Mask를 분리한다.
4. HUD는 Screen Space - Overlay Canvas에 만든다.
   몬스터 HP, 팀 시간, 스태미나, 생성 버튼, 연결 상태 자리를 배치한다.
5. 이번 HUD는 미연결 상태임을 드러내는 임시 표시다. 실제 게임 수치처럼 동작시키지 않는다.
6. 화면 분할 비율은 설정값으로 두고 첫 값은 상단 55%/하단 45%로 한다.
   모든 입력 경계는 이 실제 영역을 참조하고 별도 픽셀 상수를 만들지 않는다.
7. Canvas Scaler·앵커·Safe Area를 적용한다. 카메라 전장과 UI 안전 영역을 혼동하지 않는다.
8. 장식 UI는 입력을 가로채지 않게 하고, AudioListener·EventSystem 중복을 막는다.
9. 양쪽 기기는 같은 기본 Monster 구도를 사용한다. 좌석별 120° 카메라는 추가하지 않는다.
10. 기본 도형과 간단한 재질을 사용한다. 씬·참조를 직접 구성하거나 반복 가능한 생성 도구를 제공한다.

[완료 기준]
세로 화면에서 두 영역과 HUD가 분리되고 iOS용 프로젝트를 재생성할 수 있다.
화면 예시와 실제 렌더 결과를 구분한다.
구슬 규칙, 공격, RenderTexture·카메라 스태킹 추가는 이번 범위가 아니다.
```


## T05 · G3 · 구슬 모델·Touch·Gesture 중재

선행: T04  
사람·실기기 확인: T06에서 실제 iPhone Touch 확인

```text
현재 Task는 T05 / G3: 구슬 데이터와 2D 입력만 구현한다.
통합 PROJECT INPUT 2.4와 2.7을 사용해라.

[구현]
1. Raw/Combined, Yin/Yang/None, orbId, owner,
   권한 상태, action sequence에 필요한 최소 모델을 만든다.
   Element를 게임 규칙에 사용하지 않는다.
2. 전달·발사는 ID 유지, 조합은 새 ID라는 계약을 문서화한다.
   COMBINED라는 종류와 DRAGGING/Pending 같은 로컬 상태를 구분한다.
3. 개발용 기본·완성 구슬은 호스트 레지스트리에 등록해 공급한다.
   테스트 모드임을 표시하고 일반 최초 생성으로 기록하지 않는다.
4. 하단에서 Sprite와 Collider2D 등으로 구슬을 선택·드래그한다.
   Orb끼리 실제 물리 밀어내기는 만들지 않아도 된다.
5. Touch와 Editor Mouse를 같은 행동 경로로 연결한다.
   touchId와 UI 입력 시작을 추적하고 같은 구슬 이중 잡기를 막는다.
   동시 다중 구슬 드래그는 필수가 아니다. 추가 Pointer는 안전하게 처리한다.
6. 수평 이동 거리·방향 우세를 화면 기준 정규화 값으로 계산한다.
   초기 Threshold는 2.9의 값을 사용한다. 별도 속도 기반 플릭은 제외한다.
7. 원본 Pointer 좌표로 Gesture/Zone을 검사한 뒤 구슬 표시를 Clamp한다.
8. Attack Zone 진입·수평 Swipe·Pointer Up Drop을 구분한다.
   먼저 전환 요청을 예약한 행동에 잠그고, 같은 갱신에서 둘 다면 Zone을 우선한다.
   Raw는 공격 요청을 만들지 않는다. 아직 실제 발사·전달·조합은 실행하지 않는다.
9. 터치 취소·전투 종료·화면 이탈·잠금 중 재입력을 처리한다.
   구슬끼리 스치기만 하면 조합되는 코드를 넣지 않는다.
10. 방어 변수는 false로 예약하되 Long Press 감지는 구현하지 않는다.

[검증]
UI 중복, Touch 취소, 추가 Pointer, 모서리 행동, 잘못된 Raw 공격,
긴 수평 드래그와 조합 Drop의 충돌을 시험하고 채택 동작을 기록한다.
컴파일·자동 시험과 실제 손가락 조작은 구분한다.
```


## T06 · G3 · 2D→3D 전환과 실제 피격

선행: T05  
사람·실기기 확인: iPhone 직접 드래그 20회

```text
현재 Task는 T06 / G3: Combined를 실제 3D 공격으로 전환한다.
G2 호스트 실행과 T05의 구슬·입력 계약을 재사용해라.

[구현]
1. Combined가 Attack Zone에 처음 들어오면 발사를 요청하고 잠근다.
2. 호스트가 송신자·owner·orbId·상태·중복 requestId를 검증한다.
   Raw 요청은 거부한다. 아직 없는 전체 Battle State를 우회하는 숨은 경로는 만들지 않는다.
3. 승인 시 같은 orbId를 LAUNCHING/PROJECTILE로 바꾸고 2D 뷰를 제거한다.
   비행 전에 논리 구슬을 최종 CONSUMED로 종료하지 않는다.
4. Rigidbody와 SphereCollider를 가진 실제 3D 구슬을 생성한다.
   허수아비는 고정된 단순 Hit Collider다.
   Config 기본 피해 20, 테스트 HP 100, 속도 12, 수명 3초를 적용한다.
5. 실제로 비행·충돌해야 한다. 타이머나 즉시 HP 차감으로 명중을 대체하지 않는다.
6. 호스트만 물리·유효 피격을 확정한다.
   첫 유효 피격은 처리 완료를 먼저 표시하고 피해를 한 번 적용한다.
   클라이언트는 위치·결과를 표시하며 별도 피해를 적용하지 않는다.
7. hit 결과에 attacker와 orbId를 남긴다.
   T07의 개인 Stamina 회복을 연결할 지점을 마련하되 자원 시스템을 이 Task에서 만들지 않는다.
8. 두 기기는 같은 기본 몬스터 구도를 사용할 수 있게 한다.
   발사 계산은 입력 정규화 좌표와 명시적 발사 기준을 받는다.
   게임 규칙이 Camera.main이나 Host 화면에 묶이지 않게 한다.
9. 2D 경계의 가로 위치·크기와 3D 시작점을 맞출 조정값을 둔다.
10. 투사체끼리 충돌을 막고 빠른 발사의 관통을 실제로 시험한다.
11. 미명중·수명 만료를 정리한다.
    거절·응답 누락은 호스트 확정 상태로 확인하며 임의 복제·재발사를 허용하지 않는다.

[검증]
Raw 공격 거부, 중복 발사, 복수 충돌 이벤트, 미명중 만료를 시험한다.
AT-09/10/11/17의 부분 검증과 실제 기기 20회 직접 드래그를 기록한다.
HP 100/피해 20에서는 5회면 표적 HP가 0이 되므로
20회 시험은 테스트 표적을 명시적으로 Reset하며 진행하고 Reset 사용을 기록한다.
자동 시험과 손가락 시험을 구분하고 실기기 증거가 있어야 G3 PASS다.

생성·조합·Stamina·약점·스와이프 속도별 위력은 이번에 추가하지 마.
```


## T07 · G4 · 최초 5개 생성·추가 생성·피격 회복

선행: T06 및 G3 PASS  
사람·실기기 확인: T09에서 비용·회복을 한 판에 연결

```text
현재 Task는 T07 / G4: 합이오의 생성·개인 Stamina 규칙을 연결한다.
기존 C6의 시간 자동 회복 규칙은 사용하지 마.

[구현]
1. 단일 Config에 초기 개수 5, 최대/시작 Stamina 5,
   초기 비용 5, 추가 비용 1, 피격 회복 1, 보관 상한 20을 모은다.
2. 플레이어별 InitialBatchGenerated를 관리한다.
   최초 Tap은 Raw 5개 전체 생성과 비용 5를 한 번에 승인한다.
   이후 Tap은 Raw 1개·비용 1이다.
3. 송신자·Battle 가능 상태·자원·보관 상한·중복 요청을 확인한다.
   실패 시 자원·구슬·초기 플래그를 보존한다. 일부만 생성하지 않는다.
4. 첫 생성 전 수신 구슬이 있을 수 있음을 고려해 총 보관 수를 검사한다.
5. 일반 음양은 호스트 Seed 기반으로 결정한다.
   정확한 분포와 거부 요청의 난수 정책은 DEMO_ASSUMPTION에 기록한다.
   기본을 이전 문서의 교대 규칙으로 몰래 복원하지 않는다.
6. DEBUG_TEST_MODE에 P1 음 5개 / P2 양 5개 초기 Fixture를 둔다.
   일반 생성과 분리하고 Config·HUD·로그에 모드를 표시한다.
7. T06의 유효 피격에 실제 공격자 Stamina +1을 한 번만 연결한다.
   다른 플레이어·미명중·만료·중복 hit 알림으로는 회복하지 않는다.
8. Max를 넘지 않게 하고 생성 버튼·첫 생성 안내·Stamina·실패 이유를 갱신한다.
   초기 배치에는 필요한 비용·여유 칸 전체를 기준으로 버튼 상태를 표시한다.
9. 시간 자동 회복·무료 공급·공격자 이외 회복을 만들지 않는다.
10. 개발용 Spawn/Add Stamina는 명시적 Debug 경로로 분리한다.

[시험]
최초 5개/비용 5, 최초 중복 요청, 자원·보관 부족 시 전체 거부,
첫 Tap 전 수신 구슬, 추가 생성 비용, 성공 피격 1회 회복,
다른 사람 공격·중복 hit·빗나감의 회복 없음, Max Clamp를 검증한다.
AT-04/15/16 관련 자동 시험을 남긴다.
정식 Battle State/Reset 연결은 T09에서 마무리한다.
```


## T08 · G4 · 음양 1단계 조합

선행: T07  
사람·실기기 확인: T09에서 실제 Drop 성공·거부

```text
현재 Task는 T08 / G4: Raw Yin + Raw Yang 조합을 구현한다.
오행이나 서로 다른 속성 강제 옵션을 추가하지 마.

[구현]
1. Raw를 다른 Raw 위에 Drop하면 두 orbId와 필요한 위치로 요청한다.
2. 호스트가 서로 다른 ID, 동일 소유자, 사용 가능한 Raw,
   반대 음양을 확인한다.
3. 성공 시 재료 두 개를 동시에 소비하고 새 ID의 Combined 하나를 만든다.
   Polarity=None, CanAttack=true, CanCombine=false를 표현한다.
4. 결과 위치는 두 재료 중간점 등 중앙 사양에 정한 기준으로 처리한다.
   필요한 요청 좌표는 범위를 검증하되 로컬 2D 물리를 공유하지 않는다.
5. 실패 시 두 Raw와 자원을 유지한다.
   선택 구슬은 유효한 로컬 위치로 돌리거나 유지하고 최소 실패 피드백을 준다.
6. Raw/Combined와 Yin/Yang의 시각적 구분을 유지한다.
7. Pending·중복 요청·이미 소비된 재료·전달/발사와의 경합은
   기존 호스트 규칙으로 처리한다.
8. 결과는 T06의 실제 3D 공격을 그대로 사용한다.

[시험]
Yin+Yang 양방향 성공, 같은 음양 실패, 동일 ID 실패,
다른 소유자 실패, Raw+Combined/Combined+Combined 실패,
중복 요청·소비된 재료·실패 후 자원 보존을 검사한다.
AT-05/06을 연결하고 AT-17의 Raw 공격 거부가 유지되는지 확인한다.

추가조합·오행 배율·접촉 자동 조합은 구현하지 마.
```


## T09 · G4 · 시간형 Team HP·승패·Reset

선행: T08  
사람·실기기 확인: 한 iPhone에서 실제 Core Loop 확인

```text
현재 Task는 T09 / G4: 한 번의 전투와 초기화를 완성한다.
T06의 실제 hit와 T07/08의 생성·회복·조합을 재사용해라.

[구현]
1. BOOT/LOBBY/READY/PLAYING/VICTORY/DEFEAT와
   이후 연결할 NETWORK_ERROR를 최소 구조로 표현한다.
2. 호스트 시작 시 roundId와 종료 시각을 확정한다.
   기본은 180초, Monster HP 100이다. 개인 Stamina 5·초기 플래그 false로 시작한다.
3. Team HP는 PLAYING에서 Host Clock으로만 감소한다.
   Lobby·Ready·Result는 멈추고 몬스터 공격 감소는 없다.
4. 유효 hit는 호스트 충돌 처리 시각 < 종료 시각일 때만 처리한다.
   HP≤0 및 시간>0은 Victory, 시간≤0은 Defeat다.
   결과 결정은 같은 Tick의 콜백 순서에 따라 뒤집히지 않게 테스트한다.
5. 결과 진입 시 Clock·생성·조합·전달·발사를 막고
   잔여 투사체의 피해와 Stamina 회복도 차단한다.
6. Host만 Reset/Retry를 확정한다.
   구슬·투사체·HP·Stamina·초기 생성 플래그·Pointer/Pending·타이머를 정리한다.
   roundId를 갱신해 이전 라운드 요청을 거부한다.
7. Result에 결과·최종 HP·시간·Retry/Lobby를 표시한다.
8. 한 기기 실행은 명시적 개발 모드다. 기본 2인 시작 조건을 없애지 않는다.
9. 일반 Seed가 자원 고갈을 만들면 기록한다.
   한 기기 시험에 혼합 Fixture가 필요하면 개발용 구슬 공급으로 분리한다.

[검증]
승리, 실제 시간 경과 패배, 종료 시각의 hit, 종료 후 hit,
이전 요청, Reset 후 첫 Generate 5개를 확인한다.
AT-12/13/14와 보강 Reset 시험을 수행한다.
Force Victory/Defeat는 Result UI 디버그일 뿐 정상 판정 통과 근거가 아니다.
단축 시간 시험과 실제 180초 Clock 시험을 구분한다.
iPhone의 생성→조합→실제 hit→회복→승패→Reset 증거가 있어야 G4 PASS다.
```


## T10-A · G5 · 2인 Lobby·자동 방 탐색·Ready

선행: T09 및 G4 PASS  
사람·실기기 확인: T12에서 두 iPhone 자동 탐색·참가

```text
현재 작업은 T10의 첫 부분 T10-A다.
이번에는 Lobby와 자동 방 탐색까지만 구현한다. T10-B는 자동 시작하지 마.

[구현]
1. T02의 실제 연결을 재사용하고 Host=P1, Joiner=P2를 고정한다.
   세 번째 참가 요청은 거절한다. 양쪽에 Local Player와 좌우 상대를 표시한다.
2. Create Room, 발견 Room 목록/Join, 역할·연결·Ready, Host Start,
   Direct IP Fallback, 오류 안내를 구성한다.
3. 자동 LAN Room Discovery를 실제 구현한다.
   기존 라이브러리·설치 환경에 맞는 방식을 선택하고 선택 근거를 기록한다.
   필요한 iOS 네트워크 선언·Bonjour Service Type·Capability 등을
   실제 구현 방식의 공식 자료와 환경에서 확인한다.
   Broadcasting/Bonjour라는 이름만 기록하고 동작했다고 보고하지 않는다.
4. 탐색 시작·취소·갱신·방 종료에 따른 목록 정리를 처리한다.
   만원/다른 버전/오래된 Room 항목에 대한 참가 실패를 안내한다.
5. protocol version·build·host Config를 교환한다.
   호환 여부와 초기 상태 수신을 확인하지 않고 Ready/Start로 진행하지 않는다.
6. 두 명 준비 후 Host만 Start를 확정한다. 전투 중 새 참가를 거부한다.
   게임 전체 Snapshot 연결은 T10-B로 남기되 시작 계약을 명확히 한다.
7. 직접 IP는 탐색 실패 원인 분리·Fallback으로 유지한다.
   직접 IP 성공으로 자동 방 탐색 실패를 감추지 않는다.
8. 필요한 Info.plist·프로젝트 설정은 재현 가능하게 적용하고 실제 산출물을 검사한다.

[검증]
AT-01/02/03, 만원 참가·다른 버전·목록 만료·Ready·비Host Start 거부를 시험한다.
자동 탐색은 실제 iPhone 두 대에서 확인할 절차를 남긴다.
기기·권한·도구 문제로 탐색을 검증 못 하면 NOT_RUN/BLOCKED다.
실제 탐색이 실패하면 AT-02는 FAIL이며 IP Fallback만 PASS로 별도 기록한다.

새 외부 서버·Relay·계정·3인 확장은 추가하지 마.
```


## T10-B · G5 · 2인 게임 상태 동기화

선행: T10-A 구현·코드 검증  
사람·실기기 확인: T12에서 실제 두 iPhone

```text
현재 작업은 T10의 두 번째 부분 T10-B다.
기존 게임 규칙을 클라이언트용으로 다시 작성하지 말고
T06~T09의 호스트 처리 함수에 네트워크 입출력을 연결해라.

[구현]
1. 두 플레이어가 동일 roundId, 초기 상태, Config를 받았는지 확인하고 Start를 연결한다.
2. 생성·조합·발사를 각 기기에서 요청하고 호스트가 확정한다.
   최초 배치 플래그와 개인 Stamina도 호스트 상태다.
3. 현재 규모에 맞는 단순한 논리 상태 Snapshot을 우선 검토한다.
   session/round/revision, P1/P2·Ready, 구슬 메타데이터·owner,
   초기 플래그·자원, Monster HP, 종료 시각, Battle State를 포함한다.
4. 각 기기는 자기 보관 구슬만 2D로 표시한다.
   로컬 드래그 위치를 매 프레임 전송하지 않는다.
5. 뷰는 orbId 기준으로 추가·갱신·제거한다.
   Snapshot마다 모든 구슬을 재생성하지 않는다.
6. Pending 승인/거절을 확정 상태에 연결한다.
   응답 누락 시 권한 상태를 조회하고 임의 복구·재사용하지 않는다.
7. 3D Projectile 표시와 호스트 실제 hit를 양쪽에 연결한다.
   피해·공격자 회복은 호스트의 한 번 처리 결과만 반영한다.
8. 양쪽이 같은 플레이어의 Stamina를 비교할 수 있는 Debug 표시를 둔다.
   서로 다른 두 Local Player의 자원 값이 다르다는 이유로 동기화 실패로 보지 않는다.
9. Team HP는 공통 종료 시각 기준으로 표시하고
   허용할 표시 오차는 시험 전 기록한다. 일치 판정 기준을 시험 후 바꾸지 않는다.
10. 같은 roundId/revision의 논리 상태만 비교한다.
    낮은 revision·이전 roundId·다른 sender·잘못된 owner를 검사한다.
11. 결과와 Reset을 동기화하고 이전 라운드 요청이 새 전투를 바꾸지 않게 한다.

[검증]
각 기기의 최초 생성·추가 생성·조합·발사·회복,
동시 요청·중복 요청·초기 상태 미수신·설정 불일치를 시험한다.
해당 AT의 Editor/Host-Client 결과와 실제 Device 결과를 구분한다.
이번에는 전달·카메라 확장을 추가하지 마.
```


## T11 · G5 · 좌우 Swipe 전달

선행: T10-B  
사람·실기기 확인: T12에서 두 방향·총 100회 전달

```text
현재 Task는 T11 / G5: 2인 구조의 좌우 전달을 구현한다.
T05의 수평 Swipe와 T10-B의 호스트 상태를 재사용해라.

[구현]
1. P1/P2는 왼쪽·오른쪽 이웃이 같은 상대지만 방향 정보는 보존한다.
   Left 요청은 Receiver Right Edge, Right 요청은 Receiver Left Edge다.
2. 사용 가능한 Raw/Combined가 수평 거리·방향 우세 Threshold를 충족하면
   전달 요청을 한 번 보낸다. 별도 속도 판정은 넣지 않는다.
3. 호스트가 sender·owner·orbId·상태·상대 연결·상대 보관 상한을 확인한다.
4. 성공 시 같은 ID·종류·음양을 유지하고 owner를 한 번에 바꾼다.
   Sender와 Receiver에 동시에 조작 가능한 복제본을 만들지 않는다.
5. 높이를 정규화해 범위를 검증하고 반대 경계 안쪽에 배치한다.
   새 사용자 제스처 전에는 자동 반송하지 않는다.
6. 거부·연결 문제·응답 누락은 호스트 확정 상태로 처리한다.
   타임아웃만으로 Sender 구슬을 다시 생성하지 않는다.
7. 조합·발사·전달이 겹쳐도 같은 구슬을 두 번 소비하지 않는다.
   LAUNCHING/PROJECTILE/CONSUMED/Pending 구슬은 재전달하지 않는다.
8. 최소 나감·들어옴·성공/실패 피드백을 연결한다.

[시험]
AT-07/08/18, Raw와 Combined 양쪽 전달,
잘못된 owner·상대 만원·상대 이탈·중복 이벤트·행동 경합을 검증한다.
승인 전달 전후의 살아 있는 orbId 집합 크기는 바뀌지 않아야 한다.
T12에서 총 100회 전달을 수행할 절차를 준비한다.
양쪽 방향과 Sender를 모두 포함하는 분배를 기록하되 반복 수를 안정성 보증으로 쓰지 않는다.

연속 공유 물리·두 화면에 걸친 구슬·공동 잡기는 구현하지 마.
```


## T12 · G5 · 같은 기본 구도와 두 iPhone 전체 전투

선행: T11  
사람·실기기 확인: 실제 iPhone 두 대에서 AT-01~AT-18

```text
현재 Task는 T12 / G5: 자동 방 탐색부터 Result까지 통합한다.
3인·120° 시점 구현으로 범위를 늘리지 마.

[구현]
1. 양쪽 기기가 같은 기본 Monster 구도를 로컬 렌더링하게 한다.
   HP·전투·Projectile은 공통 권한 상태다.
2. 공격자의 정규화 가로 위치와 명시적 발사 기준으로 시작점을 계산한다.
   호스트 Camera.main을 모든 참가자의 입력 기준으로 사용하지 않는다.
3. 전달받은 Combined의 실제 투척자를 attacker로 기록한다.
   생성자·조합자와 공격자를 구분하고 공격자만 회복한다.
4. 연결·순서·Ready·Start, 최초 생성 안내, HP·시간·Stamina,
   전달·조합·공격 피드백, Result·Reset을 실제 상태로 연결한다.
5. 핵심 Loop를 막는 통합 오류만 수정한다. 새 콘텐츠·아트는 추가하지 않는다.

[실기기 Gate]
실제 iPhone 두 대, 동일 빌드·Config·같은 Wi-Fi에서 다음을 확인한다.
Create → 자동 발견 Room 선택/Join → P1/P2·양방향 이웃 확인 → Ready → Host Start
→ 각각 첫 Tap Raw 5개/비용 5
→ A가 B에게 Raw 전달 → B가 반대 음양과 조합
→ Combined를 A에게 전달 → A가 실제 발사·피격
→ Monster HP -20 및 A Stamina +1, B는 그 hit로 회복되지 않음
→ 반복 → 같은 Victory / Defeat → Reset 후 최초 배치 재생성.

일반 Seed와 P1 Yin×5 / P2 Yang×5 Fixture 사용을 분리한다.
AT-01~AT-18을 두 기기에서 수행하고 결과를 기록한다.
좌우 전달 총 100회, Raw/Combined의 정보 보존도 별도 확인한다.
실제 생성을 거치지 않은 디버그 공급은 생성 AT의 통과 근거가 아니다.
자동 탐색 대신 IP로 연결한 시험은 탐색 성공으로 기록하지 않는다.
두 Editor 창·시뮬레이터로 실제 iPhone 시험을 대체하지 않는다.

필수 기기 기준이 모두 확인돼야 G5 PASS다.
미실행·실패가 있으면 해당 AT와 전체 단계 상태를 그대로 남긴다.
```


## T13 · G6 · 앱 중단·연결 실패 처리

선행: T12 및 G5 PASS  
실기기/사람 확인: 중단·실패를 실제 기기에서 재현

```text
현재 Task는 T13 / G6: 실패 상황에서 잘못된 게임이 계속되지 않게 한다.
복구 기능을 늘리지 말고 실패 안내와 정리에 집중해라.

[정책]
전투 중 참가자 한 명이라도 이탈하면 이번 MVP의 세션을 중단한다.
호스트 교체·자동 재접속·진행 중 복귀·게임 계속하기는 구현하지 않는다.
중단 결과는 시간 만료에 의한 패배와 별도 사유로 표시한다.

[구현]
1. 호스트/클라이언트 종료, 네트워크 끊김, 연결 감시 시간 초과를 처리한다.
2. OnApplicationPause 등 앱 생명주기를 점검한다.
   호스트가 중단돼 메시지를 보내지 못해도 나머지 기기가 연결 상실을 감지하게 한다.
3. 실제 백그라운드 전환과 단순 focus 변화·권한 팝업을 구분한다.
   팝업 하나로 불필요하게 세션을 종료하는지 실기기로 시험한다.
4. 중단 시 입력을 잠그고 추가 생성·소비·피해·승패 확정을 막는다.
5. 돌아온 앱은 이전 상태로 전투를 자동 재개하지 않고 연결 화면으로 정리한다.
6. 잘못된 주소·권한 미허용·네트워크 격리 등 확인할 항목을 안내한다.
   원인이 확정되지 않은 실패를 특정 권한 문제라고 단정하지 않는다.
7. 새로 연결할 때 이전 요청·투사체·구독·NetworkManager가 남지 않게 한다.

[검증]
실기기에서 호스트 홈 이동, 클라이언트 홈 이동, 화면 잠금, 네트워크 해제,
연결 시 권한 팝업, 강제 종료, 수동 새 연결을 각각 기록한다.
코드로 생명주기 함수를 호출한 시험은 실제 iOS 중단 시험과 구분한다.
```


## T14 · G6 · 무결성·진단·자동 시험

선행: T13  
사람·실기기 확인: 실기기와 대조할 로그·시험 결과 준비

```text
현재 Task는 T14 / G6: 기존 행동을 반복 검증하고 진단 근거를 만든다.
일반 플레이를 대신하는 자동 게임을 새로 만들지 마.

[Debug 표시]
빌드·Config·Fixture·FPS·Scene·Battle/Network State·Host/Client,
Local Player·연결 수·Left/Right Player,
최근 Input/Gesture·Touch 위치·Swipe 거리/방향·선택 Orb,
Orb 상태/owner/보관·비행 수,
최근 Transfer/Combine/Attack·Monster HP·Team HP·개인 Stamina,
최근 Collision/Damage·sessionId/roundId/revision을 필요할 때 펼쳐 표시한다.

[Debug Action]
Spawn Yin/Yang/Combined, Add Stamina, Set Team HP, Damage Monster,
Force Victory/Defeat, Reset, Lobby, Clear Log를
Development Build 또는 명시적 DEBUG_MODE에서만 노출한다.
명령은 호스트가 처리하며 모두 DebugInjected로 기록한다.
정상 게임 Request의 검증을 약화해서 구현하지 않는다.

[구현과 시험]
1. 로그에 시각·role·player·requestId·orbId·sequence·roundId·revision,
   source/destination·결과·거부 사유·Fixture를 남긴다.
   크기·빈도 상한을 두고 매 프레임 파일 쓰기를 하지 않는다.
2. 순차·동시·중복·이전 라운드 요청을 재현한다.
3. 초기 배치 성공은 활성 구슬 +5/비용 5/플래그 true,
   추가 생성은 +1/비용 1, 거부는 전부 불변을 검사한다.
4. 조합은 재료 둘 종료·새 ID 하나, 전달/발사는 ID 유지,
   hit/만료는 비행 구슬 종료라는 장부를 검사한다.
   2D 뷰 수만 세어 3D 전환을 유실로 오인하지 않는다.
5. 유효 hit는 피해 한 번·공격자 회복 한 번이다.
   만료·중복 알림·종료 후 hit는 회복을 만들지 않는다.
6. 소유 오류·이중 피해·이중 생성·Reset 잔류·이전 요청을 검출한다.
7. 같은 revision의 논리 상태만 비교한다. 픽셀·표시 타이머는 제외한다.
8. EditMode/PlayMode를 분리해 테스트 XML·실행 개수·실패·로그를 저장한다.
9. AT 결과와 자동 시험을 연결하되 실제 Touch/Device를 대체하지 않는다.

테스트 0개·미실행·누락·타임아웃을 PASS로 처리하지 마.
Debug Force 결과로 AT-11/13/14/16을 통과시키지 마.
새 원격 분석 서비스·범용 프로파일러·Pool Framework는 추가하지 마.
```


## T15 · G6 · 실기기 반복·성능·화면 QA

선행: T14  
사람·실기기 확인: 실제 두 iPhone, 3분 시험 5회, 두 화면 크기

```text
현재 Task는 T15 / G6: 실제 기기의 반복 실행과 성능을 확인한다.
접근 가능한 기기·로그만 사용하고 결과를 만들어내지 마.

[준비]
1. 빌드·Config·Fixture·기기 모델·iOS·Host 역할을 고정해 기록한다.
2. 목표 60fps와 실제 측정을 구분한다. 최소 지원 기기를 추측하지 않는다.
3. Development Build는 원인 분석용,
   비개발 기기용 빌드는 실제 성능 재확인용으로 구분한다.
4. CPU/GPU frame time, 메모리, GC, 입력 지연, 연결 상태를
   가능한 도구 범위에서 측정한다. 못 얻은 항목은 NOT_RUN이다.
5. 평균 FPS 하나로 전체 성능을 PASS 처리하지 않는다.

[시나리오]
- 기본 HP/피해·생성·회복 규칙으로 두 사람의 정상 Core Loop를 반복한다.
- 실제 180초 Clock 시험을 5회 수행하고 매번 Reset 상태를 확인한다.
  기본값은 5회 유효 hit면 조기 승리할 수 있으므로,
  “3분 유지 시험”은 조기 종료하지 않는 별도 절차로 분리한다.
  공격 보류·명시적 Test Config·Debug 자원 공급 등을 사용했다면 전부 기록한다.
  짧은 Victory 5회를 3분×5회 시험으로 기록하지 않는다.
- 두 명이 동시에 생성·조합·전달·공격한다.
- 보관 상한 20개/기기와 많은 투사체의 표시·충돌·삭제를 시험한다.
  자원 주입이 필요하면 Debug 부하 시험으로 분리한다.
- 서로 다른 화면 크기의 iPhone 두 종류 이상에서
  Safe Area·Swipe/Drop·Attack Zone·전환 크기·HUD 가림을 확인한다.
- T13 중단과 T14 장부 검사, AT-01~18 회귀 결과를 대조한다.
- 후반 발열 체감·끊김은 기록하되 측정하지 않은 온도나 수치를 추정하지 않는다.

[수정]
측정된 병목·크래시·입력 오류만 먼저 수정한다.
근거 없이 대규모 Pool·Shader·렌더 구조를 교체하지 않는다.
수정 전후는 같은 기기·Config·시나리오로 비교한다.

docs/DEVICE_QA.md에 실행·미실행·Debug 사용·남은 문제를 기록한다.
빌드가 다른 로그를 하나의 통과 결과로 섞지 않는다.
직접 기기 작업을 못 하면 체크리스트를 제공하고 실제 상태는 NOT_RUN으로 둔다.
```


## T16 · G6 · 협동 관찰과 최종 판단

선행: T15의 실행 결과·미실행 항목 기록  
사람·실기기 확인: 사람의 실제 플레이 관찰

```text
현재 Task는 T16 / G6: 기술 검증과 협동 경험을 나눠 최종 보고한다.
미검증 Gate가 있다면 그대로 보고하고 전체 PASS를 만들어내지 마.

[준비]
1. docs/PLAYTEST.md에 목적·빌드·기기·Config·Seed/Fixture,
   진행 순서·관찰 항목을 기록한다.
2. 일반 Host Seed 실행과 P1 Yin×5/P2 Yang×5 Fixture를 분리한다.
   requireDifferentElements 같은 이번 범위 밖 오행 실험은 추가하지 않는다.
3. 전달이 필요했던 순간, 요청·응답, 조합 이해, 잘못 보낸 구슬,
   대기·자원 고갈·2D→3D 혼동·역할 쏠림을 관찰한다.
4. 전달·조합·공격 수는 참고하되 전달 횟수=재미로 해석하지 않는다.
   대화는 관찰자가 기록한다. 자동 음성 녹음·분석 기능은 추가하지 않는다.
5. Fixture를 사용하면 행동이 강제될 수 있음을 보고에 명시한다.
   필요 시 실행 순서를 바꿔 관찰하되 작은 표본으로 통계적 효과를 주장하지 않는다.
6. 공격자 회복만으로 자원이 고갈되는 경우를 별도 이슈로 남긴다.
   이를 해결하려고 기본 룰을 자동 회복으로 바꾸지 않는다.

[최종 보고]
docs/PROTOTYPE_REPORT.md에 다음을 정리한다.
- 실제 구현 범위·Unity/패키지·Scene/Prefab·기기·연결 조건
- G1~G6 및 AT-01~18의 상태와 증거
- Core Loop, 양방향 전달, Room Discovery, 실제 Device Validation 상태
- 일반 Seed/Fixture/Debug·시간 단축 시험을 구분한 결과
- 실제 관찰된 협동 행동·조작 문제·자원 고갈
- 실패/미실행/환경 차단과 사람의 다음 확인 순서
- 심각도별 버그·재현 순서
- 진행 / 해당 Task 보완 / 규칙 재검토 중 권고와 근거

[제한]
실행·관찰이 없으면 결과를 NOT_RUN 또는 관찰 미실시로 남긴다.
허수아비의 경험을 몬스터 패턴 전투 전체의 재미로 일반화하지 않는다.
같은 Wi-Fi 두 기기를 3~5인·공유기 없는 연결·연속 공유 물리 성공으로 표현하지 않는다.
최종 보고 후 새 기능·콘텐츠를 자동으로 시작하지 마.
```


---

# 7. 실기기 테스트 시나리오

합이오 원문의 Scenario A~D를 유지하되, 통합 결정에 맞춰 Attack Zone·ID 전환·실제 Clock 검증을 명확히 했다. 모든 시험은 빌드·Config·일반 Seed/Fixture·기기·연결 조건을 기록한다.

## Scenario A — 연결과 기본 전투 루프

1. 실제 iPhone 두 대에 같은 빌드를 설치하고 같은 로컬 Wi-Fi에 연결한다.
2. 두 앱을 실행하고 P1이 Create Room을 누른다.
3. P2에서 **실제로 발견된 Room**을 선택해 Join한다. 직접 IP를 썼다면 별도 Fallback 시험으로 기록한다.
4. 양쪽에서 Host=P1, Client=P2, Left/Right가 같은 상대임을 확인한다.
5. 두 명의 Ready와 초기 상태 수신 후 Host가 Start Battle을 누른다.
6. 양쪽이 같은 라운드의 PLAYING으로 들어가고 Team HP가 감소하는지 확인한다.
7. 각 플레이어가 Generate를 한 번 누른다. 각각 Raw 5개, 고유 ID, Stamina 0, 최초 생성 완료를 확인한다.
8. 일반 Host Seed 또는 DEBUG_TEST_MODE의 음양 분포를 기록한다.
9. P1이 Raw 하나를 오른쪽으로 Swipe한다.
10. Host 승인 후 P1의 조작 가능한 뷰가 사라지고 P2 왼쪽 경계 안쪽에 같은 ID·음양의 구슬이 나타나는지 확인한다.
11. P2가 수신 Raw와 반대 음양 Raw를 Drop한다. 재료 둘이 종료되고 새 ID의 Combined 하나가 생기는지 확인한다.
12. P2가 Combined를 Attack Zone으로 드래그한다. 한 번 요청하고 잠기는지 확인한다.
13. 승인 후 2D 뷰가 사라지고 대응하는 실제 3D Projectile이 몬스터로 이동하는지 확인한다.
14. 실제 Collider 충돌로 Monster HP가 20 감소하고 P2 Stamina가 1 회복하는지 확인한다. P1 자원은 이 hit로 회복되지 않는다.
15. 두 화면이 같은 P1/P2 자원·Monster HP·Battle State를 알고 있는지 확인한다. 각자 Local Stamina 수치가 서로 같을 필요는 없다.
16. P2가 회복한 Stamina로 추가 Generate를 누른다. Raw 하나와 비용 1을 확인한다.
17. P2가 Raw를 왼쪽으로 전달해 P1 오른쪽 경계 진입을 확인한다.
18. 다른 반복에서는 Combined 자체를 상대에게 보내고 상대가 공격하도록 한다. ID·종류 보존과 실제 투척자 회복을 확인한다.
19. 생성→전달→조합→실제 공격을 반복한다. Monster HP≤0, Team HP>0이면 양쪽 Victory를 확인한다.
20. Result의 최종 HP·시간, 입력 차단, 추가 피해·회복 차단을 확인한다.

일반 Seed 때문에 필요한 재료가 없으면 실패·대기 상황을 기록한다. 전달·조합 경로 재현이 목적이라 Fixture를 사용했다면 일반 플레이 결과와 분리한다. Debug Damage나 Force Victory는 이 시나리오의 실제 공격·승리 검증을 대신하지 않는다.

## Scenario B — 잘못된 조합과 Raw 공격

같은 기기에 Yin Raw 두 개를 준비하고 Drop한다. 재료가 유지되고 Combined가 생기지 않아야 한다. Yang 두 개도 반복한다. Raw+Combined와 Combined+Combined도 거부를 확인한다.

Raw를 Attack Zone으로 보냈을 때 발사체·피해가 없고 원본 Raw가 보존되는지 확인한다. Fixture 또는 Debug Spawn을 사용한 경우 준비 방식에만 표시하고 조합·거부 동작은 실제 Touch로 확인한다.

## Scenario C — 패배와 시간 경계

**C-1 실제 판정:** 짧은 종료 시간을 별도 Test Config로 지정하거나 실제 180초를 기다려 Host Clock이 만료되도록 한다. 양쪽 Defeat, Team HP 0, Clock·입력 정지, 잔여 발사체의 피해·회복 차단을 확인한다. 단축 시간과 180초 실행을 별도 기록한다.

**C-2 경계 시험:** 자동 시험에서 종료 직전·정확한 종료 시각·종료 후의 hit를 각각 주입해 판정 함수를 시험한다. 이는 실제 iPhone 물리 타이밍 시험을 대신하지 않는다.

**C-3 강제 Debug:** 원문의 Force Defeat는 Result UI 경로 확인용으로 유지한다. 해당 명령으로 UI가 표시됐다는 이유로 C-1이나 AT-14의 실제 Clock 판정을 통과 처리하지 않는다.

## Scenario D — Reset과 Lobby 복귀

Victory 또는 Defeat 이후 Host가 Reset/Retry를 확정한다. 구슬·투사체·HP·개인 Stamina·Clock·roundId·최초 생성 플래그·Pointer·Pending을 확인한다. 다시 첫 Generate를 누르면 Raw 5개·비용 5가 적용돼야 한다.

Lobby 복귀 후 같은 연결로 시작하는 방식 또는 명시적 새 연결 방식 중 실제 구현한 경로를 기록한다. 연결 이탈 뒤 자동으로 진행 중 전투를 복원하지 않는다. 이전 roundId 요청이 새 전투에서 작동하면 실패다.

---

# 8. Acceptance Tests와 보강 검증

## 8.1 기록 원칙

AT-01~AT-18의 번호·목적과 GIVEN/WHEN/THEN 구조는 합이오 원문에서 승계했다. 아래 코드 블록은 이번 통합 사양에 적용할 기준이다.

통합 변경점은 **AT-10의 CONSUMED 시점**이다. 공격 승인 때 2D 뷰를 제거하되 같은 논리 ID가 PROJECTILE 상태로 이어지고 명중·만료 후 CONSUMED가 된다. AT-02는 실제 방 발견·선택을 전제로 하며 직접 IP 연결로 대체하지 않는다. AT-04의 자원 소비, AT-12의 표시 오차, AT-16의 중복 회복 차단 등은 각 항목 뒤에 보강 기준을 붙였다.

각 AT는 Compile/정적 검토, EditMode, PlayMode, Host-Client, 실제 iPhone 두 대를 구분해 기록한다. 기준을 만족하는 실행을 수행한 경우만 PASS다. 검증 환경에 해당하지 않는 칸은 NOT_RUN과 이유를 남긴다. Fixture·Debug 준비·강제 판정·빌드·Config를 반드시 적는다.


### AT-01 — 파티 방 생성

```text
GIVEN
Player 1 기기에서 앱이 LOBBY 상태다.

WHEN
Player 1이 Create Room을 누른다.

THEN
새로운 Local Party Room이 생성된다.
AND
Player 1은 Host로 표시된다.
AND
Room은 Join 가능한 상태가 된다.
```

**통합 보강:** T02의 기반 Host 실행과 T10-A의 최종 Lobby 결과를 구분한다. 방을 생성했다고 자동 탐색·참가까지 PASS가 되는 것은 아니다.


### AT-02 — 방 참가

```text
GIVEN
Player 1이 Room을 생성했다.
AND
Player 2 기기가 같은 로컬 네트워크에 있다.
AND
Player 2에서 자동 탐색으로 해당 Room이 발견됐다.

WHEN
Player 2가 해당 Room을 선택해 참가한다.

THEN
동일 Party에 Player 2가 추가된다.
AND
양쪽 기기에 Player 2 연결 상태가 표시된다.
```

**통합 보강:** 자동 Discovery로 실제 발견된 Room을 선택해 참가해야 한다. 직접 IP Fallback은 별도 결과이며 이 항목의 대체 근거가 아니다.


### AT-03 — 플레이어 순서와 인접 관계

```text
GIVEN
두 기기가 같은 Party에 연결되어 있다.

WHEN
Player Order가 확정된다.

THEN
Host는 Player 1이다.
AND
Joiner는 Player 2다.
AND
각 기기는 상대를 Left와 Right Player로 인식한다.
AND
양쪽 표시가 일치한다.
```

**통합 보강:** 내부 좌석 0/1과 UI P1/P2의 매핑을 확인한다. Left/Right 수신 대상은 같아도 AT-07/08의 Entry Side는 달라야 한다.


### AT-04 — 초기 구슬 생성

```text
GIVEN
Battle State가 PLAYING이다.
AND
Local Player가 초기 생성을 하지 않았다.

WHEN
Local Player가 Generate Button을 처음 Tap한다.

THEN
Local Player 화면에 Raw Orb 5개가 생성된다.
AND
각 Orb는 고유 OrbId를 가진다.
AND
초기 생성 완료 상태가 기록된다.
```

**통합 보강:** 초기 5개와 Stamina 5 소비, 최초 플래그를 한 번에 확정한다. 중복 요청·상한 부족 시의 전체 거부는 VT-04에서 별도 검증한다.


### AT-05 — 올바른 음양 조합

```text
GIVEN
같은 기기에 Yin Raw Orb와 Yang Raw Orb가 있다.

WHEN
한 Orb를 다른 Orb 위에 Drag & Drop한다.

THEN
기존 Raw Orb 2개가 제거된다.
AND
Combined Orb 1개가 생성된다.
AND
동일 이벤트가 중복 처리되지 않는다.
```

**통합 보강:** 재료 두 ID는 종료되고 Combined는 새 ID다. Polarity=None이며 오행 조건은 추가하지 않는다.


### AT-06 — 잘못된 조합

```text
GIVEN
같은 기기에 Yin + Yin 또는 Yang + Yang Raw Orb가 있다.

WHEN
한 Orb를 다른 Orb 위에 Drag & Drop한다.

THEN
두 Orb는 제거되지 않는다.
AND
Combined Orb가 생성되지 않는다.
```

**통합 보강:** 조합 실패로 자원이 감소하거나 원본이 유실되지 않는다. 같은 음양 외 금지 조합은 T08 자동 시험에서 함께 검사한다.


### AT-07 — 좌측 전달

```text
GIVEN
두 기기가 연결되어 있다.
AND
Sender가 전달 가능한 2D Orb를 소유한다.

WHEN
Sender가 Orb를 왼쪽으로 Swipe한다.

THEN
Sender 화면에서 Orb가 제거된다.
AND
Receiver 화면의 오른쪽 경계에서 같은 OrbId의 Orb가 생성된다.
AND
Orb 데이터가 보존된다.
```

**통합 보강:** Raw와 Combined 모두 시험한다. 수신 뒤 새 사용자 Gesture 없이 자동 반송되면 실패다.


### AT-08 — 우측 전달

```text
GIVEN
두 기기가 연결되어 있다.
AND
Sender가 전달 가능한 2D Orb를 소유한다.

WHEN
Sender가 Orb를 오른쪽으로 Swipe한다.

THEN
Sender 화면에서 Orb가 제거된다.
AND
Receiver 화면의 왼쪽 경계에서 같은 OrbId의 Orb가 생성된다.
AND
Orb 데이터가 보존된다.
```

**통합 보강:** Raw와 Combined 모두 시험한다. 같은 상대라도 왼쪽 진입과 오른쪽 진입을 구분한다.


### AT-09 — 공격 시작

```text
GIVEN
Local Player가 Combined Orb를 소유한다.

WHEN
Combined Orb를 Attack Zone으로 Drag 또는 Swipe한다.

THEN
Attack Request가 Host에 전달된다.
AND
승인된 2D Combined Orb가 제거된다.
AND
동일 Orb로 두 번 공격할 수 없다.
```

**통합 보강:** Attack Zone 최초 진입으로 한 번만 요청한다. Pending 중 재전달·재조합·재발사와 중복 requestId를 검사한다.


### AT-10 — 3D 공격 변환

```text
GIVEN
Host가 유효한 Attack Request를 승인했다.

WHEN
같은 orbId가 PROJECTILE 상태로 전환되고 2D Combined Orb의 뷰가 제거된다.

THEN
대응하는 3D Projectile이 생성된다.
AND
Projectile은 Monster 방향으로 이동한다.
```

**통합 보강:** 원문 WHEN의 “2D Combined Orb가 CONSUMED 처리된다”를 “같은 orbId가 PROJECTILE로 전환되고 2D 뷰가 제거된다”로 명시적으로 수정했다. 최종 CONSUMED는 hit/만료 시점이다.


### AT-11 — 몬스터 피격

```text
GIVEN
3D Projectile이 활성 상태다.

WHEN
Projectile이 Monster Collider와 충돌한다.

THEN
Monster HP가 Base Damage만큼 감소한다.
AND
Damage는 한 번만 적용된다.
AND
Projectile은 제거된다.
```

**통합 보강:** 실제 Rigidbody 비행·Collider 충돌을 확인한다. Debug Damage, 타이머 HP 차감, Force Victory는 근거가 아니다. 회복은 AT-16으로도 확인한다.


### AT-12 — Team HP 감소

```text
GIVEN
Battle State가 PLAYING이다.

WHEN
Host Battle Clock이 진행된다.

THEN
Team HP가 시간에 따라 감소한다.
AND
두 기기에서 같은 값 또는 허용 가능한 보간 오차로 표시된다.
```

**통합 보강:** 허용할 표시 보간 오차는 시험 전에 기록한다. 양쪽의 호스트 종료 시각·라운드는 같아야 하며, Lobby/Ready/Result에서는 감소하지 않는다.


### AT-13 — 승리

```text
GIVEN
Team HP가 0보다 크다.

WHEN
Monster HP가 0 이하가 된다.

THEN
Host가 VICTORY를 판정한다.
AND
두 기기가 VICTORY 상태로 전환된다.
AND
Battle Clock과 Gameplay Input이 정지한다.
```

**통합 보강:** 종료 시각 이전의 실제 유효 hit로 조건을 충족해야 한다. Force Victory는 Result UI 시험으로만 별도 기록한다.


### AT-14 — 패배

```text
GIVEN
Battle State가 PLAYING이다.

WHEN
Team HP가 0 이하가 된다.

THEN
Host가 DEFEAT를 판정한다.
AND
두 기기가 DEFEAT 상태로 전환된다.
AND
Battle Clock과 Gameplay Input이 정지한다.
```

**통합 보강:** 실제 Host Clock 진행으로 만료를 확인한다. 정확히 0이면 Defeat다. 단축 시간과 실제 180초 시험을 분리한다.


### AT-15 — Stamina 생성 비용

```text
GIVEN
초기 생성이 끝났다.
AND
Local Player Stamina가 1 이상이다.

WHEN
Generate Button을 Tap한다.

THEN
Raw Orb 1개가 생성된다.
AND
Local Player Stamina가 1 감소한다.
```

**통합 보강:** 추가 생성에 실패하면 비용을 소비하지 않는다. 자동 회복 없이 유효 피격으로 확보한 Stamina를 사용하는 정상 경로도 확인한다.


### AT-16 — Stamina 공격 회복

```text
GIVEN
공격자의 Stamina가 Max보다 작다.

WHEN
공격자의 Projectile이 Monster에 유효하게 충돌한다.

THEN
공격자의 Stamina가 1 회복된다.
AND
다른 플레이어 Stamina는 변하지 않는다.
AND
Max Stamina를 초과하지 않는다.
```

**통합 보강:** 전달받은 Combined를 던진 실제 공격자가 회복한다. 중복 충돌·hit 알림·미명중·만료·종료 후 hit는 추가 회복을 만들지 않는다.


### AT-17 — Raw Orb 공격 거부

```text
GIVEN
Local Player가 Raw Orb를 소유한다.

WHEN
Raw Orb를 위쪽 Attack Zone으로 Drag 또는 Swipe한다.

THEN
3D Projectile이 생성되지 않는다.
AND
Monster HP가 감소하지 않는다.
AND
Raw Orb는 제거되지 않는다.
```

**통합 보강:** 원본 Raw 보존과 입력 잠금 해제를 확인한다. 공격 거부가 구슬 삭제나 영구 Pending으로 이어지면 실패다.


### AT-18 — Orb 중복 및 상태 동기화

```text
GIVEN
두 기기가 연결되어 있다.
AND
Orb Transfer 또는 Combination Event가 발생한다.

WHEN
양쪽 기기가 Host 결과를 반영한다.

THEN
하나의 OrbId는 한 시점에 하나의 유효 상태만 가진다.
AND
동일 Orb가 양쪽에 중복 조작 가능 상태로 남지 않는다.
AND
양쪽의 Monster HP, Team HP, Battle State가 일치한다.
```

**통합 보강:** 같은 roundId/revision의 논리 상태를 비교한다. 서로 다른 플레이어의 Local Stamina끼리 같아야 하는 것은 아니다. 각 플레이어 값이 양쪽에서 일치하는지 확인한다.


## 8.2 C6 완료 기준을 연결한 보강 검증

아래 VT 번호는 통합본에서 추가한 추적용 번호다. AT-01~AT-18을 대체하거나 원문에 이미 있던 시험 번호라고 표현하지 않는다.

| ID | 확인할 것 | 관련 Task | 기준 |
|---|---|---|---|
| VT-01 | 설치·수정 반영 | T01 | 실제 iPhone에서 Build A와 수정 B 구분 |
| VT-02 | 요청·승인 일치 | T03 | 두 기기의 100개 고유 요청, 중복은 1회 처리 |
| VT-03 | 2D→3D 직접 입력 | T06 | 실제 손가락 20회, Reset 사용 구분, 중복·잔류 없음 |
| VT-04 | 초기 배치 원자성 | T07 | 5개/비용/플래그 전체 성공 또는 전체 거부, 중복·수신 선행 포함 |
| VT-05 | 반복 전달 무결성 | T11/T12 | 총 100회, 양방향·양 Sender·Raw/Combined 포함, ID·정보 보존 |
| VT-06 | 늦은 요청·경계 판정·Reset | T09/T14 | 이전 roundId 거부, 0 시각 Defeat, 초기 플래그와 자원 리셋 |
| VT-07 | 앱·연결 이탈 | T13 | 어느 쪽 이탈도 NETWORK_ERROR, 결과·추가 피해·자동 복구 없음 |
| VT-08 | 부하·반복·화면 | T15 | 180초 시험 5회·두 화면 크기, Test Config/Debug 주입 구분 |
| VT-09 | 자원 고갈 | T07/T16 | 일반 Seed·미명중·고갈 관찰, 임의 자동 회복이 없음을 확인 |
| VT-10 | Gesture·UI·Pointer 중재 | T05/T11 | 한 Gesture 한 전환, Raw 공격 거부, 취소·추가 Pointer 안전 |

VT-09는 “자원이 고갈되지 않아야 통과”라는 새로운 규칙이 아니다. 원문대로 동작하는지와 고갈이 플레이에 미치는 영향을 구분해 기록하는 검증이다.

## 8.3 결과표 템플릿

아래는 빈 기록 양식이다. 실행하지 않았으므로 모든 초기 상태는 NOT_RUN이다. 작업자가 실제 시험할 때 프로젝트 문서에 복사해 증거와 함께 갱신한다.

| AT | EditMode/PlayMode | Host-Client | iPhone 2대 | 빌드·Config·증거·메모 |
|---|---|---|---|---|
| AT-01 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-02 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-03 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-04 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-05 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-06 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-07 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-08 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-09 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-10 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-11 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-12 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-13 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-14 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-15 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-16 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-17 | NOT_RUN | NOT_RUN | NOT_RUN | |
| AT-18 | NOT_RUN | NOT_RUN | NOT_RUN | |

권한 있는 Host 논리에서 기대 결과가 나왔어도 실제 UI·Touch·LAN 경로를 실행하지 않았다면 해당 iPhone 결과는 NOT_RUN이다. 네트워크 연결만 되고 구슬 전달이 실패했다면 핵심 Loop는 PARTIAL이다.

---

# 9. 공통 템플릿과 기록 경로

## 9.1 프로젝트 기록 파일

실제 프로젝트에서 아래 역할을 기록한다. 이미 동등한 문서가 있으면 내용을 병합하고 경로를 명시한다. 이 통합 파일을 받았다는 이유만으로 모든 보고 문서가 생성됐다고 보고하지 않는다.

| canonical 경로 | 내용 | 원문 경로와의 관계 |
|---|---|---|
| `AGENTS.md` | 작업 범위·안전·검증 규칙 | 기존 C6 유지 |
| `docs/PROTOTYPE_SPEC.md` | 채택 사양·Config·통합 결정·Assumption | 기존 C6 확장 |
| `docs/VALIDATION.md` | Task/Gate·빌드별 진행과 증거 | 기존 C6 유지 |
| `docs/IOS_RUNBOOK.md` | 실제 환경의 생성·빌드·서명·설치·재설치 | 기존 C6 유지 |
| `docs/ACCEPTANCE_RESULTS.md` | AT-01~18 환경별 결과·재현·증거 | `Docs/HAPIO_MVP_ACCEPTANCE_RESULTS.md` 역할 통합 |
| `docs/IOS_DEVICE_TEST_CHECKLIST.md` | Signing·권한·LAN·설치·Touch·시나리오 | `Docs/HAPIO_IOS_DEVICE_TEST_CHECKLIST.md` 역할 통합 |
| `docs/DEVICE_QA.md` | 반복·부하·화면·중단·프로파일 결과 | 기존 C6 유지 |
| `docs/PLAYTEST.md` | 일반 Seed/Fixture별 실제 관찰 | 기존 C6 유지 |
| `docs/PROTOTYPE_REPORT.md` | 구현·구조·AT·기기·문제·최종 판단 | `Docs/HAPIO_MVP_IMPLEMENTATION_REPORT.md` 역할 통합 |

기존 폴더가 `Docs`라면 프로젝트의 실제 대소문자 규칙을 우선하고 이 표와 참조를 함께 조정한다. 동일 역할의 `docs`와 `Docs`를 병렬로 만들지 않는다. 빌드 로그·테스트 XML·기기 증거는 문서에서 실제 경로를 참조하고 비밀정보를 제외한다.

## 9.2 Readiness / Inspection

```text
MVP REQUIREMENT READINESS
Task:
MVP Goal:
Success Criteria:
Core Experience:
Core Loop:
Input:
P0 Mechanics:
Rules:
Player / Device:
Network:
Win / Lose:
Scenario:
Acceptance:

Requirement Blockers:
Environment Blockers:
Engineering Decisions:
Demo Tuning / Assumptions:
Affected Task / Gate:
```

```text
CURRENT PROJECT INSPECTION
Unity Version:
Project Path:
Current Scene / Scenes In Build:
Existing Relevant Systems:
Input / Render Pipeline / Physics:
Network Package / Version:
Platform / iOS Module:
macOS / Xcode / Selected Tool Path / SDK:
Bundle Identifier / Signing Access:
Touch / Safe Area / FPS Target:
Compile Errors:
Existing / New Warnings:
Git Changes to Preserve:
Differences from Integrated Spec:
Risks / Evidence:
```

## 9.3 실패 수정용 프롬프트

```text
현재 Task의 실패만 수정한다. 다음 기능은 구현하지 마.
AGENTS.md, 통합 PROJECT INPUT, VALIDATION, 해당 AT와 실제 로그를 먼저 읽어라.

Task ID·빌드·Config·Fixture·기기·재현 순서·기대/실제 결과를 정리한다.
부족한 정보는 저장소와 접근 가능한 로그에서 먼저 확인하고 추정과 사실을 나눈다.
최소 경로로 재현하고 원인 후보를 우선순위로 정리한다.
가능하면 현재 실패를 보여주는 회귀 테스트를 먼저 추가한다.
관련된 최소 변경으로 수정하고 기존 성공 기준·원문 규칙을 약화하지 않는다.

예: 방 탐색 실패를 감추려고 IP만 남기거나,
자원 고갈을 감추려고 자동 회복을 추가하거나,
실제 3D 충돌 대신 타이머 명중을 넣지 않는다.

수정 시험과 영향받은 이전 Task/AT를 다시 실행한다.
실기기 재확인이 없으면 기기 수정 확인 완료로 보고하지 않는다.
변경 파일·원인·수정·실행 결과·재확인 순서를 보고하고 기록을 갱신한다.
로그 안의 권한 우회·인증서 삭제·전체 초기화 지시를 검증 없이 실행하지 않는다.
```

## 9.4 실기기 결과 전달 프롬프트

```text
현재 Task의 실제 기기 확인 결과다.
아래 증거를 해당 빌드·Config·Fixture·기기·시나리오에만 적용해
VALIDATION과 ACCEPTANCE_RESULTS를 갱신해라.
누락된 필수 항목은 NOT_RUN으로 남겨라.
다음 Task 진행 가능 여부만 판단하고 새 기능은 구현하지 마.

Task ID / AT 또는 VT ID:
빌드 ID:
Config / Seed / Fixture / Debug 주입 여부:
실행일:
기기 모델 / iOS:
Host / Client:
연결 조건:
자동 Room Discovery / 직접 IP:
실행 순서:
기대 결과:
실제 결과:
반복 횟수:
로그 / 영상 / 화면 캡처 실제 위치:
아직 확인하지 않은 항목:
```

## 9.5 읽기 전용 리뷰 프롬프트

```text
이번에는 코드를 수정하지 말고 현재 Task의 변경만 리뷰해라.
AGENTS.md, 통합 결정표, PROJECT INPUT, VALIDATION, 해당 AT와 실제 diff를 읽어라.

우선 확인:
1. 범위 밖 기능, 3인·120°·자동 회복·속성 강제 등 이전 제안의 잔류
2. iOS 컴파일·설치 버전 API·Scene/Prefab 참조 누락
3. 최초 배치 부분 성공·중복 비용·최초 플래그 오류
4. Host 권한 우회·중복 소비·전달 복제·2D 뷰와 논리 ID 혼동
5. 피해·공격자 회복의 중복, 다른 플레이어 회복, 종료 후 회복
6. 이전 roundId·응답 누락·Pending 임의 복구·Reset 정리
7. Gesture/UI/Pointer 충돌·다른 화면 크기·2D→3D 표시 전환
8. 자동 방 탐색과 직접 IP 결과의 혼동
9. 실제 테스트 개수·XML·Debug 주입·기기 증거·미실행의 허위 PASS
10. 비밀정보·서명 파일·불필요한 생성물의 저장소 포함

발견 사항은 심각도, 파일·위치, 재현 조건, 영향, 최소 수정 제안으로 적는다.
없으면 검토 범위와 미검증 항목을 적는다.
리뷰만으로 실기기 Gate를 PASS로 바꾸지 않는다.
```

## 9.6 임시값·범위 변경 기록

```text
DEMO_ASSUMPTION
Item:
Value / Policy:
Source: 원문 명시 / 통합 결정 / 구현 중 새 가정
Reason:
Replace Location:
Affected Task / AT:
Validation Required:
```

```text
POST-MVP CANDIDATE
Feature:
Why:
Why Not Now:
Origin: 원문 후속 / 구현 중 제안
Requires Gameplay Decision:
```

## 9.7 매 Task 완료 보고

```text
Task / Subtask:
Build / Config / Fixture:
Changed Files:
Implemented Behavior:
Requirement Conflicts / Assumptions:

Unity Compile:
EditMode / PlayMode — executed count / XML:
Host-Client Test:
Xcode Project Generation:
Xcode App Build / Signing:
iPhone Run:
Related AT / VT Results:

Evidence Paths:
NOT_RUN / BLOCKED Items and Reasons:
Manual Steps Required:
Remaining Issues:
Next Task Allowed: YES / NO — reason
```

---

# 10. 완료 정의·최종 보고·원문 추적

## 10.1 Definition of Done

구현 상태, 기기 검증 상태, 협동 관찰 상태를 각각 판단한다.

**구현 확인:** 현재 프로젝트 Compile, 필요한 Scene 실행, Input→Request→State→표시 연결, 최초/추가 생성, 조합·잘못된 조합 거부, 실제 3D 전환·충돌, 비용·공격 회복, Clock·승패·Reset이 구현되고 관련 자동 시험 결과가 기록돼야 한다.

**2인 기술 MVP 확인:** 실제 두 iPhone에서 자동 방 탐색·Join, 플레이어 순서·좌우 진입, Raw/Combined 전달, 전달받은 구슬의 조합·공격, 개인 자원·HP·Clock·같은 Result가 확인돼야 한다. AT-01~AT-18과 해당 Gate의 필수 실기기 근거가 있어야 한다.

**반복·예외 확인:** 중단 시 오류 처리, 이전 요청·중복·유실 방지, Reset, 반복·화면·성능 결과를 기록한다. 치명적 Crash·동기화 실패·미해결 필수 P0가 남으면 전체 완료로 표시하지 않는다.

**협동 관찰:** 실제 사람의 요청·교환·조합·전환 이해·대기·고갈을 기록한다. 관찰하지 않았으면 관찰 미실시다. 기술 실행과 재미의 판단을 같은 PASS로 합치지 않는다.

실제 iPhone 검증 전에는 `IMPLEMENTED — DEVICE VALIDATION PENDING` 또는 현재 부분 구현 상태로 기록한다. 직접 IP만 되고 방 탐색이 실패했다면 Room Discovery는 FAIL/BLOCKED이며 전체 기술 MVP는 PARTIAL이다.

## 10.2 최종 보고 형식

```text
MVP STATUS
Core Loop: WORKING / PARTIAL / NOT WORKING / NOT_RUN
Room Discovery: WORKING / PARTIAL / NOT WORKING / NOT_RUN
Multi-device Transfer: WORKING / PARTIAL / NOT WORKING / NOT_RUN
Physical iPhone Validation: PASS / FAIL / NOT_RUN / BLOCKED
Overall Implementation:
Cooperation Observation: 수행 / 미수행 — 관찰 범위

REQUIREMENT READINESS
Resolved Requirement Conflicts:
Remaining Requirement Blockers:
Environment Blockers:
Adopted Integration Decisions:

IMPLEMENTED / PROJECT STRUCTURE
Unity / Package Versions:
Main Scene / Build Scenes:
GameObjects / Prefabs / Scripts / Config:
Reused Existing Systems:

NETWORK ARCHITECTURE
Host Authority:
P1/P2 / Left-Right Topology:
Discovery / Direct-IP Fallback:
Orb Ownership / ID Lifecycle:
Transfer / Combination / Attack / Damage / Stamina Flow:
Clock / State / Result Sync:

DEMO ASSUMPTIONS
Item / Value / Reason / Source / Replace Location:
Seed / Fixture / Debug Injection:

VALIDATION
G1~G6 status and evidence:
AT-01~AT-18 by environment:
VT-01~VT-10:
Executed tests / XML / Device evidence:
Unexecuted items:

IOS STATUS
Target / Orientation / Touch / Safe Area:
Local Network Permission / Discovery declarations:
Xcode Project / App Build / Signing / Device Run:
Test Device Models / iOS / Host Role:

MANUAL STEPS REQUIRED
Signing / Provisioning / Device Installation:
Network / Permission / Missing Devices:

OBSERVED EXPERIENCE
General Seed Results:
Fixture Results:
Requests / Transfers / Combination Understanding:
2D→3D Confusion / Waiting / Resource Exhaustion:
Interpretation Limits:

KNOWN ISSUES
Severity / Reproduction / Impact / Build / Evidence:

NOT IMPLEMENTED / POST-MVP
Intentionally Excluded:
Pending P0:
Future Candidates:

RECOMMENDATION
진행 / 해당 Task 보완 / 규칙 재검토
Reason and Next Validation:
```

## 10.3 두 원문의 대응 관계

원문은 첨부된 다음 두 파일이다. 새 기술 조사나 Unity 프로젝트 검증을 수행한 문서로 바꾸지 않았다.

| 원문 | 승계한 내용 | 통합본 위치 |
|---|---|---|
| `C6_Codex_Prototype_Prompts.md` — 사용 방법·공통 지시 | 한 Task씩 실행, 실기기 Gate, 상태 표기, Git/환경 안전, 증거 중심 보고 | 1·4·5·9장 |
| 같은 파일 — T00~T16 | 설치·통신 선검증, 2D→3D 분리, 규칙→통합→QA 순서 | 5·6장 |
| 같은 파일 — 실패 수정·실기기 결과·리뷰 | 작업용 재사용 프롬프트 | 9장 |
| `hapio_ios_mvp_master_prompt.md` — PART 1~2 | Requirement Gate, 상세 PROJECT INPUT, 경험·우선순위·규칙·수치·화면·네트워크 | 1~4장 |
| 같은 파일 — PROJECT INPUT 24~25 | Scenario A~D, GIVEN/WHEN/THEN AT-01~18 | 7·8장 |
| 같은 파일 — PART 3~5 | Readiness, 질문·Blocker 기준, 실제 프로젝트 점검 | 3·9장 |
| 같은 파일 — PART 6 G0~G7 | 기능 의존성과 iOS 최종 검증 역할 | C6 번호로 재배치, 아래 표 |
| 같은 파일 — PART 7~15 | 책임 분리·입력·네트워크·iOS·성능·Compile/PlayMode·자동 시험·범위 통제 | 2~4·6·9장 |
| 같은 파일 — PART 16~20 | DoD·결과 문서·최종 보고·우선순위·시작 흐름 | 1·5·9·10장 |

### 합이오 원문의 G번호를 찾는 표

| 합이오 원문 단계 | 이 통합본에서 수행 |
|---|---|
| G0 iOS Build Spike | T00~T01 / G0~G1 |
| G1 Single-Instance Core Combat | T04~T09 / G3~G4 |
| G2 Local Party Connection | T02~T03 / G2, T10-A / G5 |
| G3 Orb Network Ownership and Transfer | T05, T10-B, T11 |
| G4 Networked Combination and Attack | T06~T08, T10-B, T12 |
| G5 Shared HP and Result | T09, T10-B, T12 |
| G6 Debug and Automated Verification | 각 Task의 관련 시험, T14 |
| G7 Two-iPhone Device Test | G1~G5의 단계별 실기기 Gate, T12~T15 |

### 공식 자료의 취급

기존 C6 문서의 공식 참고 자료 섹션은 기술 배경 참고로 유지한다. 이 통합 작업에서 해당 페이지를 새로 열어 최신성을 검증한 것은 아니다. 실제 구현 시 설치 Unity·패키지·Xcode와 맞는 공식 문서를 확인하고 그때의 확인 결과를 프로젝트에 기록한다. 원문의 링크 버전을 설치·업그레이드 지시로 취급하지 않는다.


- OpenAI, AGENTS.md: https://developers.openai.com/codex/guides/agents-md
- Unity, Editor command line arguments: https://docs.unity3d.com/6000.0/Documentation/Manual/EditorCommandLineArguments.html
- Unity, URP split-screen rendering: https://docs.unity3d.com/6000.0/Documentation/Manual/urp/rendering-to-the-same-render-target.html
- Unity, Netcode physics: https://docs.unity3d.com/Packages/com.unity.netcode.gameobjects@2.7/manual/advanced-topics/physics.html
- Unity, authority: https://mp-docs.dl.it.unity3d.com/netcode/current/terms-concepts/authority/
- Unity Test Framework, command-line tests: https://docs.unity3d.com/Packages/com.unity.test-framework@1.4/manual/reference-command-line.html
- Apple, TN3179 local network privacy: https://developer.apple.com/documentation/technotes/tn3179-understanding-local-network-privacy


**최종 실행 원칙:** 이 파일을 읽었다면 현재 상태를 확인하고 지정 Task 하나만 수행한다. 실기기 증거가 없으면 결과를 채우지 않는다. 완료되지 않은 P0를 후속 기능으로 숨기지 않는다. 설명보다 실제 프로젝트 변경과 확인 가능한 실행 근거를 남긴다.
