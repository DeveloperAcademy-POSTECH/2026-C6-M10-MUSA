# MUSA / C6 Unity prototype 작업 안내

## 현재 기준과 범위

- 저장소 루트의 Assets·Packages·ProjectSettings가 실제 Unity 프로젝트다. 특정 개인의 데스크탑 절대 경로를 사용하지 않는다.
- 이관 원본은 C6 프로토타입 빌드24, 기준 소스 커밋 `66b6dc163c3c8f4f2f1c1212f35734a6bb7bd6ae`의 현재 파일이다. 별도 저장소의 과거 Git 이력은 병합하지 않았다.
- 먼저 README.md, docs/MIGRATION.md, docs/VALIDATION_SUMMARY.md, docs/P4_IMPLEMENTATION.md, 실제 Git 변경과 해당 하위 지시를 확인한다.
- 최신 사용자 지시를 우선하며 이번 PR은 구현 이관이다. 새 기능·원문 미실행 Task·main 병합을 자동 시작하지 않는다.
- 루트의 통합 마스터 문서와 과거 T/P 문서는 당시 요구와 기록이다. 최신 README·P4 규칙보다 앞선 제한(2인만·좌우 벽 반발·상단 진입 즉시 발사)을 현재에 강제하지 않는다.

## 현재 게임 규칙

- 같은 Wi-Fi의 2~5인 Host/Client, 원형 이웃, 전원 Ready와 초기 상태 확인을 사용한다.
- 시작 구슬0·개인 자원100/100, Raw1개 생성 비용20, 시간 회복3초당20을 연속 계산, 유효 명중의 실제 공격자만 +5(상한100)다.
- 자연 물리 접촉으로 조합하지 않는다. 직접 드래그 정상 종료에서 Yin+Yang 조합을 요청한다.
- 자유 이동 구슬의 좌우 끝 도달은 자동 전달하며 속도·높이·동일ID를 유지한다. 상하 벽과 구슬에는 반발, 시간 경과에는 감속을 적용한다. 수신 한도20 거부 때 원본 끝에서 정지한다.
- Combined의 손떼기 방향·세기로 3D 투척하고 Host Rigidbody/Collider의 실제 유효 피격만 피해20을 적용한다. 몬스터 HP100·한 판180초를 유지한다.
- Host 승인·송신자/소유자·세션/라운드/요청·중복 검증과 공유 등록부를 우회하지 않는다. 전달·발사는 ID 유지, 조합은 재료2 소비와 새ID다.
- 팀 기획의 오행·상성·몬스터 AI·실제 방어는 이번 코드에 구현됐다고 취급하지 않는다.

## 보존과 확인

- 기존 파일·씬·Prefab·Config·.meta·사용자 변경·팀 README와 .github 양식을 보존한다.
- Unity6000.5.7f1·URP17.5.0·저장 패키지 버전을 임의 업그레이드하거나 렌더러를 교체하지 않는다.
- 같은 프로젝트의 Editor가 열려 있으면 중복 batchmode를 실행하지 않는다. 프로세스 강제 종료, lock/Library 삭제, 사용자 변경 되돌리기를 하지 않는다.
- 씬/에셋 연결 변경에는 Unity Editor API 또는 제공된 준비 도구를 사용한다. 현재 P4 빌드·실행 절차는 docs/P4_RUNBOOK.md를 따른다.
- Config 저장 원본은 하나이며 런타임 복사본을 구분한다. 모든 Config 필드가 Host 자동 전송 대상인 것은 아니다.
- 문서 승인·소스 존재·파일 이관은 실행 PASS가 아니다. Compile, EditMode, PlayMode, 다인 실행, iOS 출력·서명·설치·Touch를 따로 기록한다.
- PASS/FAIL/NOT_RUN/BLOCKED와 버전·기기 종류·수치·실행 근거를 구분한다. 과거 PASS를 새 경로·새 수정의 PASS로 승계하지 않는다.
- 이 저장소는 공개다. 비밀번호·개인 키·토큰·서명 인증서·기기 일련번호·개인 서명자 정보·원시 실행 로그를 커밋하지 않는다. 공개 가능한 검증 요약을 작성한다.
- docs/를 사용하고 Docs/를 중복 생성하지 않는다. 변경 내용·검증 범위·사람 작업을 한국어로 보고한다.

## 로컬 네트워크 개선 계획 (2026-09-15)

- 최신 사용자 결정은 추가 패키지 없이 핫스팟·일반 Wi-Fi 호환성을 개선하고 인터넷 없는 LAN 플레이를 유지하는 것이다. Photon·Relay 전환은 현재 실행 범위가 아니다.
- 2026-09-15 후속 사용자 요청의 L1 진단·전송 경로 검증을 완료했다. Mac 자동 검사36개와 iPhone 핫스팟↔iPad IPv6 왕복의 실제 근거는 docs/L1_NETWORK_DIAGNOSTICS.md를 확인한다. 진단 경로7개 중 성공3·실패4를 그대로 보존한다. L2 로비·게임 통합은 별도 다음 작업이다.
- L1은 명시적인 `C6_L1_PROBE` 플레이어 define으로만 켜는 진단 앱이다. 일반 Editor define에 추가하지 않는다. 게임 씬·규칙과 패키지 버전은 유지하며, 진단 데이터 왕복을 게임 JOIN 성공으로 기록하지 않는다.
- 중단된 N1 임시 코드는 미완성·미검증이다. 통째로 병합하거나 실행 완료로 취급하지 않는다. 게임 응답 제한과 접속 시도 제한의 공유 참조를 확인하고 게임 규칙을 보존한다.

- 최신 후속 요청으로 L2 탐색·참가 통합을 구현했다. 앱26 / 게임 계약24이며 후보 재시도·취소·IPv4/IPv6 Host 수신·최초 상태 확인을 적용한다. 실제 결과는 docs/L2_CONNECTION_INTEGRATION.md와 docs/validation/L2_20260915.json을 확인한다. 자동/Mac/iOS 빌드·설치와 실제 iPhone 핫스팟→iPad 게임 JOIN·초기 상태·양쪽 Ready 확인까지 통과했다. L2 범위만 완료됐으며 전체 게임·다인·다른 실제 네트워크는 별도 검증이다.
- L2 추가 실행 도구는 `C6_L2_CHECKS` Development 빌드에서만 자동 연결된다. 기기에서는 관찰만, Mac에서는 명시 인자에 한해 실행한다. L3 게임·3~5인 회귀 및 L4 다른 실제 네트워크 시험을 자동 시작하지 않는다.

- 후속 요청으로 L3를 압축 실행해 완료했다. 전체 Edit1,417·Play146, IPv4/IPv6 혼합 Mac3/5인 각1회 통과. 게임 변경 없이 P4 진단 주소 인자와 실행 도구만 추가했다. 범위/미실행은 docs/L3_GAME_REGRESSION.md를 확인하며 L4는 별도 요청이다.
