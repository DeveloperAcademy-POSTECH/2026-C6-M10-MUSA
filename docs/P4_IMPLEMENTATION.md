> **공개 이관본 안내:** 이 문서의 개인 경로·서명 식별자는 예시로 일반화했습니다. `/path/to/...`, `YOUR_TEAM_ID`, `HOST_IPV4`는 자신의 환경 값으로 바꾸세요. 원시 증거와 과거 문서 묶음은 공개하지 않으며 [검증 요약](VALIDATION_SUMMARY.md)·[이관 범위](MIGRATION.md)에서 구분합니다. 과거 기록의 완료·미완료 상태는 새 실행 결과가 아닙니다.

# P4 좌우 경계 연속 이동 · 구현 결정

2026-09-14 사용자는 Unity 공식 스킬의 유용성을 확인하고 다음 작업을 시작하도록 요청했다. 기준은 `691c4cc`의 빌드23이다. 기존 좌우 반발·수동 전달·정지 수신을 새 씬 `ContinuousTransferBattle` / 빌드24에서 변경한다. 이전 씬·Config·패키지·실기기 증거는 보존한다.

| 항목 | 이번 결정 |
|---|---|
| 좌우 경계 | Raw/Combined의 자유 관성 이동이 경계에 도달하면 Host에게 자동 전달 요청. 반발하지 않음 |
| 잡고 있는 구슬 | 손에 붙어 유지. 손을 놓으면 관성 이동을 시작하며 자동 통과. 정지한 손떼기는 속도0이므로 이동하지 않음 |
| 속도와 높이 | 두 축 속도 모두 해당 구슬 영역 전체 너비/초 단위. 높이는 중심이 이동 가능한 영역의 상대 높이. 수신 시 반대쪽 경계에 배치 |
| 마찰 | 기존 Config의 0.6 너비/초²·정지 기준0.015 너비/초 유지. 경계 관측→Host 승인→수신까지 NGO ServerTime으로 지연 마찰을 적용 |
| 통신 중 | 보낸 화면 끝에서 승인 대기. 승인된 현재 소유자만 움직이며 이전 물리 뷰 제거. 지연 동안 위치를 예측해 이동시키지는 않음 |
| 순서·중복 | 동일 OrbID/단일 Host 소유권. transferCount가 새로 증가한 수신 회차에서만 속도를 적용 |
| 가득 찬 이웃 | 기존20개 한도 유지. 거부 시 보내는 쪽 끝에서 멈춤. 자동 재요청·반발 없음. 다시 밀면 새 요청 가능 |
| 도착 위치 점유 | 동일 상대 높이에 도착하고 실제 Collider 충돌로 해결. 빈 위치로 임의 재배치하지 않음 |
| 유지하는 규칙 | 직접 드래그 정상 손떼기에서만 Yin+Yang 조합. 상하 반발·구슬끼리 충돌·손떼기3D 투척·고정 회전·자원·최대5인 유지 |
| 프로토콜 | Lobby/Game24·P4 및 Attack CE24 이름으로 구 버전과 혼합 방지 |
| 요청 검증 | 발신자·소유권·방향·경계·수신 용량·순서·유한값·시각 검증. 과거8초/미래0.25초 허용. 충돌 속도 여유는 최대 손떼기 속도×16; 에너지 추가·속도 고정 없음 |

시간·속도 허용 경계는 DEMO_TUNING_VALUE다. 회전이나 모든 화면의 위치를 Host가 매 프레임 시뮬레이션하는 새 구조는 추가하지 않았다. 단일 Config에서 기존 마찰과 속도 설정을 읽는다.

## 공식 Unity 스킬 적용

[Unity-Technologies/skills](https://github.com/Unity-Technologies/skills)의 커밋 `645158263ad5f93296ca54444db8f2ec5dbf8c4d`를 확인했다. 물리 전용 스킬은 없어 프로젝트 실행·빌드·테스트에 유용한 `unity-cli`만 설치했다. 다른 온라인 서비스·UI·패키지 스킬은 이번 범위에 적용하지 않는다. 설치 위치는 `/path/to/developer-home/.codex/skills/unity-cli`; 자동 탐색은 다음 턴부터 가능하며 이번에는 읽은 지침을 적용한다.

설치된 CLI는1.0.0-beta.8, 스킬 설명은beta.9 기준도 포함하므로 실제 `--help`로 지원 인수를 확인했다. `unity status`는 Pipeline 연결0개를 반환했지만 실제 원본 Editor는 열려 있다. Pipeline 패키지를 임의 추가하지 않고 별도 Git 작업 폴더에서 Editor API와 CLI batch 절차를 사용한다. CLI 강제 종료 timeout·자동 설치·업그레이드 옵션은 사용하지 않는다. 다운로드 설치의 인증서 검증 오류 후 인증서 검증을 끄지 않고 Git 방식으로 고정 커밋을 설치했다.

## 검증 계획

1. 기존 회귀와 경계·지연 마찰·Host 승인·중복·용량·계약·새 씬 자동 검사. 테스트 XML의 실행 수·실패·누락 확인.
2. 실제 Mac 2/3/5개 독립 앱과 직접 IP에서 한 번 밀어 여러 경계 통과→마찰 정지, 좌우 Raw/Combined, 손 유지, 수신 한도 거부, Retry. DEBUG_POINTER/Fixture이며 실제 Touch·Bonjour와 구분.
3. iOS 출력·빌드·서명은 별도 결과로 기록. 실제 기기 설치/화면/Touch는 실행 증거가 있을 때만 PASS.

현재 실행 결과는 `P4_VALIDATION.md`에 별도 기록하며 과거 PASS를 승계하지 않는다.
